# Lightweight development environment for the knowledge-summary POC.
# Resolve with uv against requirements.lock and requirements-ci.lock.
# This environment does not support app.main or the shared backend test suite.
fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.10.6
python-dotenv==1.0.1
psycopg[binary]==3.3.4
pytest==8.3.4
httpx==0.28.1
