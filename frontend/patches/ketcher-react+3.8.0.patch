diff --git a/node_modules/ketcher-react/dist/Editor.d.ts b/node_modules/ketcher-react/dist/Editor.d.ts
--- a/node_modules/ketcher-react/dist/Editor.d.ts
+++ b/node_modules/ketcher-react/dist/Editor.d.ts
@@ -1,6 +1,8 @@
-import { EditorProps } from './MicromoleculesEditor';
-declare type Props = Omit<EditorProps, 'ketcherId'> & {
-    disableMacromoleculesEditor?: boolean;
-};
-export declare const Editor: (props: Props) => import("react/jsx-runtime").JSX.Element;
-export {};
+import { EditorProps } from './MicromoleculesEditor';
+declare type Props = Omit<EditorProps, 'ketcherId'> & {
+    disableMacromoleculesEditor?: boolean;
+    /** Prepare Macro only when an operation requires it. */
+    deferMacromoleculesEditor?: boolean;
+};
+export declare const Editor: (props: Props) => import("react/jsx-runtime").JSX.Element;
+export {};
diff --git a/node_modules/ketcher-react/dist/cjs/Editor.d.ts b/node_modules/ketcher-react/dist/cjs/Editor.d.ts
--- a/node_modules/ketcher-react/dist/cjs/Editor.d.ts
+++ b/node_modules/ketcher-react/dist/cjs/Editor.d.ts
@@ -1,6 +1,8 @@
-import { EditorProps } from './MicromoleculesEditor';
-declare type Props = Omit<EditorProps, 'ketcherId'> & {
-    disableMacromoleculesEditor?: boolean;
-};
-export declare const Editor: (props: Props) => import("react/jsx-runtime").JSX.Element;
-export {};
+import { EditorProps } from './MicromoleculesEditor';
+declare type Props = Omit<EditorProps, 'ketcherId'> & {
+    disableMacromoleculesEditor?: boolean;
+    /** Prepare Macro only when an operation requires it. */
+    deferMacromoleculesEditor?: boolean;
+};
+export declare const Editor: (props: Props) => import("react/jsx-runtime").JSX.Element;
+export {};
diff --git a/node_modules/ketcher-react/dist/cjs/index.js b/node_modules/ketcher-react/dist/cjs/index.js
--- a/node_modules/ketcher-react/dist/cjs/index.js
+++ b/node_modules/ketcher-react/dist/cjs/index.js
@@ -147,7 +147,13 @@
 function createApi(structServiceProvider, defaultOptions) {
   var _structService$destro;
   var structService = structServiceProvider.createStructService(defaultOptions);
-  var info = structService.info();
+  var info = structService.info().then(function (value) { return value; });
+  Object.defineProperty(info, 'nexpolyMacroController', {
+    get: function () { return structService.nexpolyMacroController; },
+    set: function (controller) { structService.nexpolyMacroController = controller; }
+  });
+  info.catch(function () {});
+  Object.defineProperty(info, "disposed", { get: function () { return structService.disposed; } });
   return Object.assign(info, {
     addKetcherId: structService.addKetcherId.bind(structService),
     info: structService.info.bind(structService),
@@ -519,6 +525,11 @@
         }()
       };
       Object.keys(this.listeners).forEach(function (en) {
+        var handler = _this2.listeners[en];
+        _this2.listeners[en] = function (event) {
+          if (_this2.disposed || !nexpolyOwnsEvent(el.parentElement, event)) return;
+          return handler(event);
+        };
         _this2.target.addEventListener(en, _this2.listeners[en]);
       });
     }
@@ -531,6 +542,8 @@
     key: "componentWillUnmount",
     value: function componentWillUnmount() {
       var _this3 = this;
+      this.disposed = true;
+      this.props.dispose?.();
       Object.keys(this.listeners).forEach(function (en) {
         _this3.target.removeEventListener(en, _this3.listeners[en]);
       });
@@ -3289,7 +3302,10 @@
       _options.rescale;
       _options.fragment;
       var formatterOptions = _objectWithoutProperties__default["default"](_options, _excluded$B);
-    var format = ketcherCore.identifyStructFormat(struct);
+    var controller = server.nexpolyMacroController;
+    var declaredFormat = controller?.detectInputFormat(struct, options['input-format']);
+    var format = controller?.requiresMacroFormat(declaredFormat) ? 'ket' : ketcherCore.identifyStructFormat(struct);
+    if (declaredFormat) formatterOptions['input-format'] = declaredFormat;
     if (format === ketcherCore.SupportedFormat.cdx) {
       struct = "base64::".concat(struct.replace(/\s/g, ''));
     }
@@ -3321,10 +3337,26 @@
   return selection;
 };
 function load(struct, options) {
+  return function (dispatch, getState) {
+    var controller = getState().server?.nexpolyMacroController;
+    if (!controller) return nexpolyLoadStructure(struct, options)(dispatch, getState);
+    return controller.enqueue(async function () {
+      var result = await nexpolyLoadStructure(struct, options)(dispatch, getState);
+      controller.check();
+      if (controller.macro && controller.macro._type !== (controller.view ? 1 : 0)) await controller.setMode(controller.view);
+      return result;
+    }).catch(function (error) {
+      if (error.name !== 'AbortError') getState().editor.errorHandler?.(String(error));
+    });
+  };
+}
+function nexpolyLoadStructure(struct, options) {
   return function () {
     var _ref = _asyncToGenerator__default["default"]( _regeneratorRuntime__default["default"].mark(function _callee(dispatch, getState) {
+      var nexpolyController = getState().server?.nexpolyMacroController;
       var state, editor, server, serverSettings, errorHandler, _options2, isPaste, method, otherOptions, parsedStruct, _otherOptions, fragment, hasUnsupportedGroups, hasMoleculeToMonomerConnections, oldStruct, isIndigoFunctionCalled;
       return _regeneratorRuntime__default["default"].wrap(function _callee$(_context) {
+        nexpolyController?.check();
         while (1) switch (_context.prev = _context.next) {
           case 0:
             state = getState();
@@ -8772,11 +8804,11 @@
   var dialogRef = React.useRef(null);
   React.useLayoutEffect(function () {
     if (focusable) {
-      dialogRef.current.focus();
+      nexpolyFocus(dialogRef.current);
     }
     return function () {
       var _dialogRef$current;
-      (_dialogRef$current = dialogRef.current) === null || _dialogRef$current === void 0 || (_dialogRef$current = _dialogRef$current.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)) === null || _dialogRef$current === void 0 || _dialogRef$current.getElementsByClassName(CLIP_AREA_BASE_CLASS)[0].focus();
+      (_dialogRef$current = dialogRef.current) === null || _dialogRef$current === void 0 || (_dialogRef$current = _dialogRef$current.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)) === null || _dialogRef$current === void 0 || _dialogRef$current.getElementsByClassName(CLIP_AREA_BASE_CLASS)[0]?.focus({ preventScroll: true });
     };
   }, [focusable]);
   var isButtonOk = function isButtonOk(button) {
@@ -14877,6 +14909,7 @@
   return function (dispatch, getState) {
     var onResize = fp.throttle(250, function () {
       var state = getState();
+      if (!state.editor || state.editor.disposed) return;
       state.editor.render.update();
       dispatch({
         type: 'CLEAR_VISIBLE',
@@ -14884,6 +14917,7 @@
       });
     });
     addEventListener('resize', onResize);
+    return function () { removeEventListener('resize', onResize); onResize.cancel(); };
   };
 }
 function toolbarReducer () {
@@ -15029,6 +15063,7 @@
 });
 function getRootReducer(setEditor) {
   return function root(state, action) {
+    if (state?.editor?.disposed) return state;
     switch (action.type) {
       case 'INIT':
         {
@@ -15148,25 +15183,46 @@
 function ownKeys$18(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
 function _objectSpread$18(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys$18(Object(t), !0).forEach(function (r) { _defineProperty__default["default"](e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$18(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
 var throttleMilliseconds = 100;
-function useThrottleResizeObserver() {
-  var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
-  var _useState = React.useState({
-      height: undefined,
-      width: undefined
-    }),
-    _useState2 = _slicedToArray__default["default"](_useState, 2),
-    size = _useState2[0],
-    setSize = _useState2[1];
-  var onResize = React.useMemo(function () {
-    return _.throttle(setSize, throttleMilliseconds);
-  }, []);
-  var _useResizeObserver = useResizeObserver__default["default"](_objectSpread$18({
-      onResize: onResize
-    }, options)),
-    ref = _useResizeObserver.ref;
-  return _objectSpread$18({
-    ref: ref
-  }, size);
+// A ref callback must not enqueue setState(null) while this nested root unmounts.
+// React Refresh otherwise re-registers the second, empty commit and retains it.
+function nexpolyUseInView(options) {
+  var state = React.useState(false);
+  var observation = React.useRef(null);
+  var ref = React.useCallback(function (element) {
+    observation.current?.disconnect();
+    observation.current = null;
+    if (!element) return;
+    var observer = new IntersectionObserver(function (entries) {
+      if (observation.current !== observer || !element.isConnected) return;
+      var entry = entries[0];
+      state[1](entry.isIntersecting && entry.intersectionRatio >= options.threshold);
+    }, options);
+    observation.current = observer;
+    observer.observe(element);
+  }, [options.threshold]);
+  React.useLayoutEffect(function () { return function () { ref(null); }; }, [ref]);
+  return [ref, state[0]];
+}
+function useThrottleResizeObserver(options = {}) {
+  var state = React.useState({ height: undefined, width: undefined });
+  var elementRef = React.useRef(null);
+  var ref = React.useCallback(function (element) { elementRef.current = element; }, []);
+  React.useLayoutEffect(function () {
+    var element = options.ref?.current || elementRef.current;
+    if (!element) return;
+    var active = true;
+    var onResize = _.throttle(function (size) {
+      if (active && element.isConnected && element.getClientRects().length) state[1](size);
+    }, throttleMilliseconds);
+    var observer = new ResizeObserver(function (entries) {
+      if (!active || !element.isConnected || !element.getClientRects().length) return;
+      var rect = entries[0]?.contentRect;
+      if (rect) onResize({ width: Math.round(rect.width), height: Math.round(rect.height) });
+    });
+    observer.observe(element);
+    return function () { active = false; observer.disconnect(); onResize.cancel(); };
+  }, [options.ref]);
+  return { ref: ref, ...state[0] };
 }
 
 function useFormContext() {
@@ -15250,14 +15306,10 @@
   useInterval(scrollBack, isScrollDown ? 100 : null);
   useInterval(scrollForward, isScrollUp ? 100 : null);
   React.useEffect(function () {
-    return function () {
-      setScrollUp(false);
-    };
+    setScrollUp(false);
   }, [startInView]);
   React.useEffect(function () {
-    return function () {
-      setScrollDown(false);
-    };
+    setScrollDown(false);
   }, [endInView]);
   return jsxRuntime.jsxs("div", {
     className: clsx__default["default"](classes$L.scroll, isLeftRight ? classes$L.leftRight : classes$L.upDown),
@@ -15355,13 +15407,13 @@
     ref = _useResizeObserver.ref,
     height = _useResizeObserver.height;
   var scrollRef = React.useRef(null);
-  var _useInView = reactIntersectionObserver.useInView({
+  var _useInView = nexpolyUseInView({
       threshold: 1
     }),
     _useInView2 = _slicedToArray__default["default"](_useInView, 2),
     startRef = _useInView2[0],
     startInView = _useInView2[1];
-  var _useInView3 = reactIntersectionObserver.useInView({
+  var _useInView3 = nexpolyUseInView({
       threshold: 1
     }),
     _useInView4 = _slicedToArray__default["default"](_useInView3, 2),
@@ -15649,13 +15701,13 @@
   var _useResizeObserver = useThrottleResizeObserver(),
     ref = _useResizeObserver.ref,
     height = _useResizeObserver.height;
-  var _useInView = reactIntersectionObserver.useInView({
+  var _useInView = nexpolyUseInView({
       threshold: 1
     }),
     _useInView2 = _slicedToArray__default["default"](_useInView, 2),
     startRef = _useInView2[0],
     startInView = _useInView2[1];
-  var _useInView3 = reactIntersectionObserver.useInView({
+  var _useInView3 = nexpolyUseInView({
       threshold: 1
     }),
     _useInView4 = _slicedToArray__default["default"](_useInView3, 2),
@@ -20229,21 +20281,18 @@
   return _isChangingFunctionalGroup.apply(this, arguments);
 }
 
-var keydownListener = null;
 function initKeydownListener(element) {
   return function (dispatch, getState) {
     var hotKeys = ketcherCore.initHotKeys(configWithNonViewOnlyActionsDisabled);
-    keydownListener = function keydownListener(event) {
+    var keydownListener = function keydownListener(event) {
+      if (!nexpolyOwnsEvent(element, event)) return;
       return keyHandle(dispatch, getState, hotKeys, event);
     };
     element.addEventListener('keydown', keydownListener);
-  };
-}
-function removeKeydownListener(element) {
-  return function () {
-    if (keydownListener) {
+    return function () {
       element.removeEventListener('keydown', keydownListener);
-    }
+      clearTimeout(getState().editor?.abbreviationLookupTimeoutId);
+    };
   };
 }
 function removeNotRenderedStruct(actionTool, group, dispatch) {
@@ -20252,7 +20301,6 @@
     dispatch(removeStructAction());
   }
 }
-var abbreviationLookupTimeoutId;
 var ABBREVIATION_LOOKUP_TYPING_TIMEOUT = 1000;
 var shortcutKeys = ['1', '2', '3', '4', 't', 'h', 'n', 'o', 's', 'p', 'f', 'i', 'b', '+', '-'];
 function keyHandle(dispatch, getState, hotKeys, event) {
@@ -20275,16 +20323,16 @@
     var isAbbreviationLookupShown = (!isTheSameKey || !isShortcutKey) && currentlyPressedKeys;
     if (isAbbreviationLookupShown) {
       dispatch(showAbbreviationLookup(event.key));
-      clearTimeout(abbreviationLookupTimeoutId);
-      abbreviationLookupTimeoutId = undefined;
+      clearTimeout(editor.abbreviationLookupTimeoutId);
+      editor.abbreviationLookupTimeoutId = undefined;
       var resetAction = ketcherCore.SettingsManager.getSettings().selectionTool;
       dispatch(onAction(resetAction));
       event.preventDefault();
       return;
     } else {
-      abbreviationLookupTimeoutId = window.setTimeout(function () {
+      editor.abbreviationLookupTimeoutId = window.setTimeout(function () {
         dispatch(closeAbbreviationLookup());
-        abbreviationLookupTimeoutId = undefined;
+        editor.abbreviationLookupTimeoutId = undefined;
       }, ABBREVIATION_LOOKUP_TYPING_TIMEOUT);
       dispatch(initAbbreviationLookup(event.key));
     }
@@ -20396,6 +20444,7 @@
   });
   return {
     formats: formats,
+    dispose: function () { debAction.cancel(); loadStruct.cancel(); },
     focused: function focused() {
       var state = getState();
       return !state.modal;
@@ -20649,9 +20698,10 @@
     staticResourcesUrl = _useSettingsContext.staticResourcesUrl;
   React.useEffect(function () {
     if (typeof requestIdleCallback === 'function') {
-      requestIdleCallback(function () {
+      var idle = requestIdleCallback(function () {
         return onInitTmpls(ref.current, staticResourcesUrl);
       });
+      return function () { cancelIdleCallback(idle); };
     } else {
       onInitTmpls(ref.current, staticResourcesUrl);
     }
@@ -24311,6 +24361,17 @@
     clientArea.addEventListener('mouseleave', this.onMouseLeave);
   }
   _createClass__default["default"](HoverIcon, [{
+    key: "destroy",
+    value: function destroy() {
+      if (!this.editor) return;
+      var area = this.editor.render.clientArea;
+      document.removeEventListener('mousemove', this.onMouseMove);
+      area.removeEventListener('mouseover', this.onMouseMove);
+      area.removeEventListener('mouseleave', this.onMouseLeave);
+      this.element.remove();
+      this.element = this.editor = null;
+    }
+  }, {
     key: "fill",
     get: function get() {
       return this._fill;
@@ -24338,6 +24399,7 @@
   }, {
     key: "onMouseMove",
     value: function onMouseMove(event) {
+      if (!this.editor || this.editor.disposed || !nexpolyOwnsEvent(this.editor.render.clientArea, event)) return;
       if (this.isShown || this.isOverLoader(event) && this.shouldBeShownWhenMouseBack) {
         this.show();
         this.updatePosition();
@@ -25166,6 +25228,23 @@
     this.render.paper.canvas.setAttribute('data-testid', 'canvas');
   }
   _createClass__default["default"](Editor, [{
+    key: "destroy",
+    value: function destroy() {
+      if (this.destroyed) return;
+      this.destroyed = this.disposed = true;
+      this.domDisposers.splice(0).forEach(function (dispose) { dispose(); });
+      clearTimeout(this.abbreviationLookupTimeoutId);
+      clearTimeout(this._tool?.connectedPreviewTimeout);
+      this.hoverIcon.destroy();
+      Object.values(this.event).forEach(function (subscription) { subscription.handlers = []; });
+      this.render.disposed = true;
+      clearTimeout(this.render.updateTimer);
+      this.render.unobserveCanvasResize();
+      this.render.scrollbar.destroy();
+      this.render.paper.remove();
+      this.historyStack = [];
+    }
+  }, {
     key: "isDitrty",
     value: function isDitrty() {
       var position = this.historyPtr;
@@ -25479,6 +25558,20 @@
   }, {
     key: "openMonomerCreationWizard",
     value: function openMonomerCreationWizard() {
+      var owner = ketcherCore.ketcherProvider.getKetcher();
+      var controller = owner?.structService.nexpolyMacroController;
+      if (controller && controller.state !== 'ready') {
+        if (this.nexpolyWizardPending) return this.nexpolyWizardPending;
+        var chosen = JSON.stringify(this.selection());
+        var revision = controller.revision;
+        this.nexpolyWizardPending = controller.ensure().then(() => {
+          controller.check();
+          if (!this.disposed && controller.revision === revision && JSON.stringify(this.selection()) === chosen) return this.openMonomerCreationWizard();
+        }).catch(error => { if (error.name !== 'AbortError') this.errorHandler?.(String(error)); })
+          .finally(() => { this.nexpolyWizardPending = null; });
+        return this.nexpolyWizardPending;
+      }
+
       var _selection$atoms2,
         _this2 = this;
       var currentStruct = this.render.ctab.molecule;
@@ -26089,8 +26182,8 @@
   }, {
     key: "focusCliparea",
     value: function focusCliparea() {
-      var cliparea = document.querySelector('.cliparea');
-      cliparea === null || cliparea === void 0 || cliparea.focus();
+      var cliparea = this.render.clientArea.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)?.querySelector('.cliparea');
+      nexpolyFocus(cliparea);
     }
   }], [{
     key: "isSelectionContinuous",
@@ -26192,7 +26285,25 @@
   }
   return false;
 }
+function nexpolyFocus(element) {
+  if (!element || !element.isConnected || !element.getClientRects().length) return;
+  var active = document.activeElement;
+  var root = element.closest('[data-structure-editor]') || element.closest(KETCHER_ROOT_NODE_CSS_SELECTOR);
+  if (active && active !== document.body && !root?.contains(active) &&
+      (active.matches('input,textarea,select') || active.isContentEditable)) return;
+  element.focus({ preventScroll: true });
+}
+function nexpolyOwnsEvent(element, event) {
+  var root = element.closest?.('[data-structure-editor]') || element;
+  if (!element.isConnected || !element.getClientRects().length || !root.isConnected || !root.getClientRects().length || root.closest('[inert], [hidden]')) return false;
+  var target = event.target;
+  if (element.contains(target)) return true;
+  var popup = target?.closest?.('[data-ketcher-owner]');
+  return popup?.getAttribute('data-ketcher-owner') === root.getAttribute('data-ketcher-session');
+}
 function domEventSetup(editor, clientArea) {
+  editor.domDisposers = [];
+  var dragging = false;
   var trackedDomEvents = [{
     target: clientArea,
     eventName: 'click',
@@ -26232,10 +26343,17 @@
       toolEventHandler = _ref13.toolEventHandler;
     editor.event[eventName] = new subscription.DOMSubscription();
     var subs = editor.event[eventName];
-    target.addEventListener(eventName, function () {
-      if (window.isPolymerEditorTurnedOn) return;
-      subs.dispatch.apply(subs, arguments);
-    });
+    var callback = function (event) {
+      if (editor.disposed || window.isPolymerEditorTurnedOn) return;
+      var owned = nexpolyOwnsEvent(clientArea, event);
+      if (eventName === 'mousedown' && owned) dragging = true;
+      var finishDrag = eventName === 'mouseup' || eventName === 'mouseleave';
+      if (!owned && !dragging) return;
+      if (finishDrag) dragging = false;
+      subs.dispatch(event);
+    };
+    target.addEventListener(eventName, callback);
+    editor.domDisposers.push(function () { target.removeEventListener(eventName, callback); subs.handlers = []; });
     subs.add(function (event) {
       updateLastCursorPosition(editor, event);
       if (['mouseup', 'mousedown', 'click', 'dbclick'].includes(event.type) && !isMouseMainButtonPressed(event)) {
@@ -29551,7 +29669,7 @@
     value: function componentWillUnmount() {
       removeEditorHandlers(this.editor, this.props);
       this.editorRef.current.removeEventListener('wheel', this.handleWheel);
-      this.editor.render.unobserveCanvasResize();
+      this.editor.destroy();
     }
   }, {
     key: "render",
@@ -33420,7 +33538,8 @@
     setTimeout(function () {
       onOk({
         structStr: structStr,
-        fragment: false
+        fragment: false,
+        'input-format': fileName ? ketcherCore.getFormatMimeTypeByFileName(fileName) : undefined
       });
       setIsLoading(false);
     }, 0);
@@ -36961,7 +37080,7 @@
     (_inputRef$current = inputRef.current) === null || _inputRef$current === void 0 || _inputRef$current.focus();
     return function () {
       var _inputRef$current2;
-      (_inputRef$current2 = inputRef.current) === null || _inputRef$current2 === void 0 || (_inputRef$current2 = _inputRef$current2.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)) === null || _inputRef$current2 === void 0 || _inputRef$current2.getElementsByClassName(CLIP_AREA_BASE_CLASS)[0].focus();
+      (_inputRef$current2 = inputRef.current) === null || _inputRef$current2 === void 0 || (_inputRef$current2 = _inputRef$current2.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)) === null || _inputRef$current2 === void 0 || _inputRef$current2.getElementsByClassName(CLIP_AREA_BASE_CLASS)[0]?.focus({ preventScroll: true });
     };
   }, []);
   var closePanel = function closePanel() {
@@ -37164,15 +37283,15 @@
 var handleMouseMove = function handleMouseMove(dispatch, event) {
   dispatch(updateCursorPosition(event.clientX, event.clientY));
 };
-var pointerMoveListener = null;
-var mouseDownListener = null;
 function initMouseListener(element) {
   return function (dispatch, getState) {
     var throttledHandleMouseMove = _.throttle(handleMouseMove, MOUSE_MOVE_THROTTLE_TIMEOUT);
-    pointerMoveListener = function pointerMoveListener(event) {
+    var pointerMoveListener = function pointerMoveListener(event) {
+      if (!nexpolyOwnsEvent(element, event)) return;
       return throttledHandleMouseMove(dispatch, event);
     };
-    mouseDownListener = function mouseDownListener(event) {
+    var mouseDownListener = function mouseDownListener(event) {
+      if (!nexpolyOwnsEvent(element, event)) return;
       var areBothLeftAndRightButtonsClicked = event.buttons === 3;
       if (areBothLeftAndRightButtonsClicked) {
         handleRightClick(getState);
@@ -37180,16 +37299,11 @@
     };
     element.addEventListener('pointermove', pointerMoveListener);
     element.addEventListener('mousedown', mouseDownListener, true);
-  };
-}
-function removeMouseListeners(element) {
-  return function () {
-    if (pointerMoveListener) {
+    return function () {
       element.removeEventListener('pointermove', pointerMoveListener);
-    }
-    if (mouseDownListener) {
-      element.addEventListener('mousedown', mouseDownListener, true);
-    }
+      element.removeEventListener('mousedown', mouseDownListener, true);
+      throttledHandleMouseMove.cancel();
+    };
   };
 }
 function handleRightClick(getState) {
@@ -37215,9 +37329,9 @@
       store.dispatch(setServer(structService));
     };
   };
-  store.dispatch(initKeydownListener(element));
-  store.dispatch(initMouseListener(element));
-  store.dispatch(initResize());
+  var disposeKeys = store.dispatch(initKeydownListener(element));
+  var disposeMouse = store.dispatch(initMouseListener(element));
+  var disposeResize = store.dispatch(initResize());
   appRoot.render(jsxRuntime.jsx(reactRedux.Provider, {
     store: store,
     children: jsxRuntime.jsx(settingsContext.Provider, {
@@ -37241,8 +37355,10 @@
     })
   }));
   return function () {
-    store.dispatch(removeKeydownListener(element));
-    store.dispatch(removeMouseListeners(element));
+    disposeKeys();
+    disposeMouse();
+    disposeResize();
+    if (global.currentState?.editor === store.getState().editor) delete global.currentState;
   };
 }
 
@@ -37282,25 +37398,20 @@
   }, {
     key: "reinitializeApi",
     value: function reinitializeApi(ketcherId, structServiceProvider, setStructServiceToStore) {
-      var _this = this;
-      var oldStructService = this.structService;
-      this.structService = createApi(structServiceProvider, ketcherCore.DefaultStructServiceOptions);
-      this.structService.addKetcherId(ketcherId);
-      window.addEventListener(STRUCT_SERVICE_INITIALIZED_EVENT, function () {
-        var _oldStructService$des;
-        oldStructService === null || oldStructService === void 0 || (_oldStructService$des = oldStructService.destroy) === null || _oldStructService$des === void 0 || _oldStructService$des.call(oldStructService);
-        if (!_this.structService) {
-          ketcherCore.KetcherLogger.warn('Structure service is not reinitialized');
-          return;
-        }
+      var builder = this;
+      var oldService = this.structService;
+      var service = this.structService = createApi(structServiceProvider, ketcherCore.DefaultStructServiceOptions);
+      service.addKetcherId(ketcherId);
+      service.info().then(function () {
+        if (builder.disposed || builder.structService !== service) { service.destroy?.(); return; }
         var ketcher = ketcherCore.ketcherProvider.getKetcher(ketcherId);
-        ketcher.reinitializeIndigo(_this.structService);
-        IndigoProvider.setIndigo(_this.structService);
-        setStructServiceToStore(_this.structService);
-      }, {
-        once: true
-      });
-      return this.structService;
+        if (!ketcher || ketcher.disposed) { service.destroy?.(); return; }
+        oldService?.destroy?.();
+        ketcher.reinitializeIndigo(service);
+        IndigoProvider.setIndigo(service);
+        setStructServiceToStore(service);
+      }).catch(function (error) { service.destroy?.(); if (!builder.disposed) console.error(error); });
+      return service;
     }
   }, {
     key: "appendServiceMode",
@@ -37370,50 +37481,46 @@
       var params = new URLSearchParams(document.location.search);
       var initialMol = params.get('moll');
       if (initialMol) {
-        ketcher.setMolecule(initialMol);
+        if (ketcher.structService.nexpolyMacroController) ketcher.nexpolyInitialMol = initialMol;
+        else ketcher.setMolecule(initialMol);
       }
       IndigoProvider.setIndigo(this.structService);
       ketcherCore.ketcherProvider.addKetcherInstance(ketcher);
+      window.ketcher = ketcher;
       return ketcher;
     }
   }]);
   return KetcherBuilder;
 }();
 
-function buildKetcherAsync(_x) {
-  return _buildKetcherAsync.apply(this, arguments);
-}
-function _buildKetcherAsync() {
-  _buildKetcherAsync = _asyncToGenerator__default["default"]( _regeneratorRuntime__default["default"].mark(function _callee(_ref) {
-    var element, appRoot, staticResourcesUrl, structServiceProvider, buttons, errorHandler, togglerComponent, customButtons, prevKetcherId, builder, structService, ketcher, _yield$builder$append, cleanup, setServer;
-    return _regeneratorRuntime__default["default"].wrap(function _callee$(_context) {
-      while (1) switch (_context.prev = _context.next) {
-        case 0:
-          element = _ref.element, appRoot = _ref.appRoot, staticResourcesUrl = _ref.staticResourcesUrl, structServiceProvider = _ref.structServiceProvider, buttons = _ref.buttons, errorHandler = _ref.errorHandler, togglerComponent = _ref.togglerComponent, customButtons = _ref.customButtons, prevKetcherId = _ref.ketcherId;
-          builder = new KetcherBuilder();
-          structService = builder.appendApiAsync(structServiceProvider);
-          builder.appendServiceMode(structServiceProvider.mode);
-          ketcher = builder.build();
-          structService.addKetcherId(ketcher.id);
-          _context.next = 8;
-          return builder.appendUiAsync(prevKetcherId, ketcher.id, element, appRoot, staticResourcesUrl, errorHandler, buttons, togglerComponent, customButtons);
-        case 8:
-          _yield$builder$append = _context.sent;
-          cleanup = _yield$builder$append.cleanup;
-          setServer = _yield$builder$append.setServer;
-          return _context.abrupt("return", {
-            ketcher: ketcher,
-            cleanup: cleanup,
-            builder: builder,
-            setServer: setServer
-          });
-        case 12:
-        case "end":
-          return _context.stop();
-      }
-    }, _callee);
-  }));
-  return _buildKetcherAsync.apply(this, arguments);
+async function buildKetcherAsync(props) {
+  var builder = new KetcherBuilder();
+  var service = builder.appendApiAsync(props.structServiceProvider);
+  builder.appendServiceMode(props.structServiceProvider.mode);
+  if (props.createMacroController) builder.structService.nexpolyMacroController = props.createMacroController();
+  var ketcher = builder.build();
+  service.addKetcherId(ketcher.id);
+  props.onBuild(ketcher, builder);
+  var cleanup;
+  var abort;
+  try {
+    var result = await new Promise(function (resolve, reject) {
+      abort = function () { reject(new DOMException('Editor initialization disposed', 'AbortError')); };
+      props.signal.addEventListener('abort', abort, { once: true });
+      cleanup = initApp(props.ketcherId, ketcher.id, props.element, props.appRoot,
+        props.staticResourcesUrl, { buttons: props.buttons || {}, errorHandler: props.errorHandler || null,
+          version: '3.8.0', buildDate: '2025-10-17T11:21:57', buildNumber: '', customButtons: props.customButtons || [] },
+        service, resolve, props.togglerComponent);
+      props.onCleanup(cleanup);
+      if (props.signal.aborted) abort();
+    });
+    if (props.signal.aborted) throw new DOMException('Editor initialization disposed', 'AbortError');
+    result.editor.errorHandler = typeof props.errorHandler === 'function' ? props.errorHandler : function () {};
+    return { ketcher: ketcher, builder: builder, cleanup: cleanup, setServer: result.setServer };
+  } catch (error) {
+    ketcher.retire();
+    throw error;
+  } finally { props.signal.removeEventListener('abort', abort); }
 }
 
 var styles = {"button-common-styles":"Editor-module_button-common-styles__h4pal","scrollbar":"Editor-module_scrollbar__yVsY8","small":"Editor-module_small__8cgEX","editor":"Editor-module_editor__o0Gjz","-hidden":"Editor-module_-hidden__aKodt","switchingLoader":"Editor-module_switchingLoader__OlmM6","blink":"Editor-module_blink__J7UCV","editorsWrapper":"Editor-module_editorsWrapper__qMSvd"};
@@ -37424,85 +37531,71 @@
   smallWidth: 1040,
   smallHeight: 600
 };
+// Serialize old root cleanup before creating another SDK root, including StrictMode.
+var nexpolyRootCleanup = Promise.resolve();
 function MicromoleculesEditor(props) {
-  var initPromiseRef = React.useRef(null);
-  var appRootRef = React.useRef(null);
-  var cleanupRef = React.useRef(null);
-  var ketcherBuilderRef = React.useRef(null);
-  var setServerRef = React.useRef(function () {});
-  var structServiceProvider = props.structServiceProvider;
   var rootElRef = React.useRef(null);
-  var _useResizeObserver = useThrottleResizeObserver({
-      ref: rootElRef
-    }),
-    height = _useResizeObserver.height,
-    width = _useResizeObserver.width;
+  var active = React.useRef(null);
+  var latestProps = React.useRef(props);
+  latestProps.current = props;
+  var dimensions = useThrottleResizeObserver({ ref: rootElRef });
   React.useEffect(function () {
-    var _ketcherBuilderRef$cu;
-    if (!props.ketcherId) {
-      return;
-    }
-    (_ketcherBuilderRef$cu = ketcherBuilderRef.current) === null || _ketcherBuilderRef$cu === void 0 || _ketcherBuilderRef$cu.reinitializeApi(props.ketcherId, props.structServiceProvider, setServerRef.current);
-  }, [structServiceProvider]);
-  var initKetcher = function () {
-    var _ref = _asyncToGenerator__default["default"]( _regeneratorRuntime__default["default"].mark(function _callee() {
-      var _initPromiseRef$curre;
-      return _regeneratorRuntime__default["default"].wrap(function _callee$(_context) {
-        while (1) switch (_context.prev = _context.next) {
-          case 0:
-            appRootRef.current = client.createRoot(rootElRef.current);
-            initPromiseRef.current = buildKetcherAsync(_objectSpread$1(_objectSpread$1({}, props), {}, {
-              element: rootElRef.current,
-              appRoot: appRootRef.current
-            }));
-            (_initPromiseRef$curre = initPromiseRef.current) === null || _initPromiseRef$curre === void 0 || _initPromiseRef$curre.then(function (_ref2) {
-              var _props$onSetKetcherId;
-              var ketcher = _ref2.ketcher,
-                cleanup = _ref2.cleanup,
-                builder = _ref2.builder,
-                setServer = _ref2.setServer;
-              cleanupRef.current = cleanup;
-              ketcherBuilderRef.current = builder;
-              setServerRef.current = setServer;
-              (_props$onSetKetcherId = props.onSetKetcherId) === null || _props$onSetKetcherId === void 0 || _props$onSetKetcherId.call(props, ketcher.id);
-              if (typeof props.onInit === 'function' && ketcher) {
-                props.onInit(ketcher);
-                var ketcherInitEvent = new Event(ketcherInitEventName(ketcher.id));
-                window.dispatchEvent(ketcherInitEvent);
-              }
-            });
-          case 3:
-          case "end":
-            return _context.stop();
-        }
-      }, _callee);
-    }));
-    return function initKetcher() {
-      return _ref.apply(this, arguments);
-    };
-  }();
+    var current = active.current;
+    if (current?.builder && current.provider !== props.structServiceProvider) {
+      current.provider = props.structServiceProvider;
+      current.builder.reinitializeApi(current.ketcher.id, current.provider, current.setServer);
+    }
+  }, [props.structServiceProvider]);
   React.useEffect(function () {
-    if (initPromiseRef.current === null) {
-      initKetcher();
-    } else {
-      var _initPromiseRef$curre2;
-      (_initPromiseRef$curre2 = initPromiseRef.current) === null || _initPromiseRef$curre2 === void 0 || _initPromiseRef$curre2["finally"](function () {
-        initKetcher();
-      });
-    }
+    var record = { disposed: false, root: null, cleanup: null, ketcher: null, builder: null,
+      provider: props.structServiceProvider, controller: new AbortController() };
+    active.current = record;
+    var element = rootElRef.current;
+    var start = nexpolyRootCleanup.then(async function () {
+      if (record.disposed) return;
+      var appRoot = { render: function (view) {
+        if (record.disposed) return;
+        record.root = client.createRoot(element, { onUncaughtError: function (error) {
+          latestProps.current.errorHandler?.(error.message || String(error));
+          record.controller.abort();
+        } });
+        // Create and commit the nested root atomically. An initialization failure
+        // before render never registers an empty root with React Refresh.
+        ReactDOM__default["default"].flushSync(function () { record.root.render(view); });
+      } };
+      try {
+        var result = await buildKetcherAsync({ ...latestProps.current, element: element, appRoot: appRoot,
+          signal: record.controller.signal,
+          onBuild: function (ketcher, builder) { record.ketcher = ketcher; record.builder = builder; },
+          onCleanup: function (cleanup) { record.cleanup = cleanup; } });
+        if (record.disposed || active.current !== record) { result.ketcher.retire(); return; }
+        record.setServer = result.setServer;
+        latestProps.current.onSetKetcherId?.(result.ketcher.id);
+        latestProps.current.onInit?.(result.ketcher);
+        window.dispatchEvent(new Event(ketcherInitEventName(result.ketcher.id)));
+      } catch (error) {
+        if (!record.disposed) latestProps.current.errorHandler?.(error.message || String(error));
+      }
+    });
     return function () {
-      var _initPromiseRef$curre3;
-      (_initPromiseRef$curre3 = initPromiseRef.current) === null || _initPromiseRef$curre3 === void 0 || _initPromiseRef$curre3.then(function () {
-        var _cleanupRef$current, _appRootRef$current;
-        (_cleanupRef$current = cleanupRef.current) === null || _cleanupRef$current === void 0 || _cleanupRef$current.call(cleanupRef);
-        (_appRootRef$current = appRootRef.current) === null || _appRootRef$current === void 0 || _appRootRef$current.unmount();
+      record.disposed = true;
+      record.controller.abort();
+      record.ketcher?.retire();
+      if (record.builder) { record.builder.disposed = true; record.builder.structService?.destroy?.(); }
+      // Defer nested root unmount until React has finished the host commit.
+      nexpolyRootCleanup = Promise.all([nexpolyRootCleanup, start]).then(function () {
+        record.cleanup?.();
+        record.root?.unmount();
+        if (window.ketcher === record.ketcher) delete window.ketcher;
+        if (IndigoProvider.getIndigo() === record.ketcher?.structService) IndigoProvider.setIndigo(undefined);
+        record.root = record.cleanup = record.builder = record.ketcher = null;
+        if (active.current === record) active.current = null;
       });
     };
   }, []);
-  return jsxRuntime.jsx("div", {
-    ref: rootElRef,
-    className: clsx__default["default"](KETCHER_ROOT_NODE_CLASS_NAME, styles.editor, _defineProperty__default["default"]({}, styles.small, height && height <= mediaSizes.smallHeight || width && width <= mediaSizes.smallWidth))
-  });
+  return jsxRuntime.jsx('div', { ref: rootElRef,
+    className: clsx__default["default"](KETCHER_ROOT_NODE_CLASS_NAME, styles.editor,
+      { [styles.small]: dimensions.height && dimensions.height <= mediaSizes.smallHeight || dimensions.width && dimensions.width <= mediaSizes.smallWidth }) });
 }
 
 var _templateObject, _templateObject2, _templateObject3, _templateObject4, _templateObject5, _templateObject6, _templateObject7, _templateObject8, _templateObject9;
@@ -37617,125 +37710,278 @@
 
 function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
 function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty__default["default"](e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
-var MacromoleculesEditorComponent = React.lazy(function () {
-  return Promise.resolve().then(function () { return require('./index.modern-8e10d5ac.js'); });
-});
+const aborted = () => new DOMException('Editor initialization disposed', 'AbortError');
+const macroTypes = new Set(['monomer', 'ambiguousMonomer', 'monomerTemplate',
+  'ambiguousMonomerTemplate', 'monomerGroupTemplate']);
+const macroFormats = new Map([
+  ['helm', 'helm'], ['chemical/x-helm', 'helm'], ['fasta', 'fasta'], ['chemical/x-fasta', 'fasta'],
+  ['sequence', 'sequence'], ['chemical/x-sequence', 'sequence'],
+  ['sequence3Letter', 'sequence-3-letter'], ['sequence-3-letter', 'sequence-3-letter'],
+  ['chemical/x-peptide-sequence-3-letter', 'sequence-3-letter'],
+  ['idt', 'idt'], ['chemical/x-idt', 'idt'],
+  ['chemical/x-rna-sequence', 'sequence'], ['chemical/x-dna-sequence', 'sequence'],
+  ['chemical/x-peptide-sequence', 'sequence']
+]);
+
+function containsMacro(source) {
+  if (typeof source !== 'string' || !source.trim().startsWith('{')) return false;
+  const document = JSON.parse(source);
+  return [...Object.values(document), ...(document.root?.nodes || []), ...(document.root?.templates || [])]
+    .some(value => value && typeof value === 'object' && macroTypes.has(value.type));
+}
+
+/** One controller per SDK, registered before any initial document writes. */
+class MacroController {
+  constructor({ load, show, fail, deferred = true }) {
+    Object.assign(this, { load, show, fail, deferred });
+    this.state = 'idle';
+    this.generation = 0;
+    this.tail = Promise.resolve();
+    this.waiters = new Set();
+    this.view = false;
+  }
+  check() { if (this.state === 'disposed' || this.sdk?.disposed) throw aborted(); }
+  attach(sdk) {
+    this.check(); this.sdk = sdk;
+    this.revision = 0;
+    this.onChange = () => { ++this.revision; };
+    sdk.changeEvent?.add(this.onChange);
+  }
+  requiresMacroFormat(format) { return macroFormats.has(format); }
+  detectInputFormat(source, explicit) {
+    if (explicit) return explicit;
+    if (/^\s*(?:PEPTIDE|RNA|CHEM|BLOB)\d+\s*\{/.test(source)) return 'helm';
+    if (/^\s*>/.test(source)) return 'fasta';
+  }
+  enqueue(action) {
+    try { this.check(); } catch (error) { return Promise.reject(error); }
+    const task = this.tail.then(() => { this.check(); return action(); });
+    this.tail = task.catch(() => {});
+    return this.guard(task);
+  }
+  guard(task) {
+    this.check();
+    return new Promise((resolve, reject) => {
+      const stop = error => reject(error);
+      this.waiters.add(stop);
+      Promise.resolve(task).then(value => { this.check(); resolve(value); }, reject)
+        .catch(reject).finally(() => this.waiters.delete(stop));
+    });
+  }
+  ensure() {
+    try { this.check(); } catch (error) { return Promise.reject(error); }
+    if (this.state === 'ready') return Promise.resolve(this.macro);
+    if (this.pending) return this.pending.promise;
+    const generation = ++this.generation;
+    this.state = 'loading';
+    let resolve, reject;
+    const promise = new Promise((yes, no) => { resolve = yes; reject = no; });
+    promise.catch(() => {});
+    const timer = setTimeout(() => this.rejectAttempt(new Error('Macro initialization timed out'), generation), 15000);
+    this.pending = { promise, resolve, reject, timer, generation };
+    Promise.resolve().then(() => this.load(generation)).catch(error => this.rejectAttempt(error, generation));
+    return promise;
+  }
+  initialized(editor, generation) {
+    if (this.state === 'disposed' || generation !== this.generation || !this.pending) {
+      editor.destroy(); return;
+    }
+    this.macro = editor;
+    // A hidden virgin Macro does not own the small-molecule document.
+    editor._type = 0;
+    this.state = 'ready';
+    const pending = this.pending;
+    this.pending = null;
+    clearTimeout(pending.timer);
+    pending.resolve(editor);
+  }
+  rejectAttempt(error, generation) {
+    if (generation !== this.generation || this.state === 'disposed') return;
+    const pending = this.pending;
+    this.pending = null;
+    this.state = 'failed';
+    if (pending) { clearTimeout(pending.timer); pending.reject(error); }
+    this.fail(error, generation);
+  }
+  async setMode(value) {
+    this.check();
+    const macro = value ? await this.ensure() : this.macro;
+    this.check();
+    if (macro && !macro.disposed && macro._type !== (value ? 1 : 0)) {
+      if (value) macro.switchToMacromolecules();
+      else macro.switchToMicromolecules();
+    }
+    this.view = value;
+    window.isPolymerEditorTurnedOn = value;
+    // The host acknowledges a command id, even if the boolean did not change.
+    await this.guard(this.show(value));
+    if (!value) this.sdk?.editor?.focusCliparea();
+  }
+  requestMode(value) { return this.enqueue(() => this.setMode(value)); }
+  async normalize(source, format, options) {
+    this.check();
+    const explicit = this.detectInputFormat(source, format);
+    if (macroFormats.has(explicit) || containsMacro(source)) await this.ensure();
+    if (macroFormats.has(explicit)) {
+      const sequenceType = /^chemical\/x-(rna|dna|peptide)-sequence$/.exec(explicit)?.[1].toUpperCase();
+      const result = await this.sdk.structService.convert({ struct: source,
+        input_format: macroFormats.get(explicit), output_format: 'chemical/x-indigo-ket' },
+      { ...options, ...(sequenceType ? { 'sequence-type': sequenceType } : {}) });
+      this.check();
+      return { source: result.struct, format: 'ket' };
+    }
+    // Formats that may hide monomers are normalized only during an actual import.
+    if (['mol', 'molV3000', 'cdxml', 'smilesExt', 'chemical/x-mdl-molfile', 'chemical/x-cdxml'].includes(explicit)) {
+      const result = await this.sdk.structService.convert({ struct: source, output_format: 'chemical/x-indigo-ket' });
+      this.check();
+      if (containsMacro(result.struct)) { await this.ensure(); return { source: result.struct, format: 'ket' }; }
+    }
+    return { source, format };
+  }
+  import(source, options, original) {
+    return this.enqueue(async () => {
+      const normalized = await this.normalize(source, options?.inputFormat, options);
+      this.check();
+      const result = await original(normalized.source, options?.inputFormat || normalized.format
+        ? { ...options, inputFormat: normalized.format } : options);
+      this.check();
+      // Importers can change model ownership without changing the visible mode.
+      if (this.macro && this.macro._type !== (this.view ? 1 : 0)) await this.setMode(this.view);
+      return result;
+    });
+  }
+  dispose() {
+    if (this.state === 'disposed') return;
+    ++this.generation;
+    this.state = 'disposed';
+    this.sdk?.changeEvent?.remove(this.onChange);
+    const error = aborted();
+    if (this.pending) { clearTimeout(this.pending.timer); this.pending.reject(error); this.pending = null; }
+    for (const reject of this.waiters) reject(error);
+    this.waiters.clear();
+    // SDK caches can retain an old service wrapper. Retired capabilities must
+    // not keep its document, React callbacks or editor reachable through it.
+    this.sdk = this.macro = this.onChange = undefined;
+    this.load = this.show = this.fail = () => {};
+    this.tail = Promise.resolve();
+  }
+}
+
+function nexpolyLoadMacroModule() { return import("./index.modern-8e10d5ac.js"); }
+// Embedded in both SDK module formats by the audited r4 patch.
+class NexPolyMacroBoundary extends React.Component {
+  constructor(props) { super(props); this.state = { failed: false }; }
+  static getDerivedStateFromError() { return { failed: true }; }
+  componentDidCatch(error) { this.props.onError(error); }
+  render() { return this.state.failed ? null : this.props.children; }
+}
 var Editor = function Editor(props) {
-  var _useState = React.useState(false),
-    _useState2 = _slicedToArray__default["default"](_useState, 2),
-    showPolymerEditor = _useState2[0],
-    setShowPolymerEditor = _useState2[1];
-  var _useState3 = React.useState(),
-    _useState4 = _slicedToArray__default["default"](_useState3, 2),
-    moleculesEditor = _useState4[0],
-    setMoleculesEditor = _useState4[1];
-  var _useState5 = React.useState(),
-    _useState6 = _slicedToArray__default["default"](_useState5, 2),
-    ketcher = _useState6[0],
-    setKetcher = _useState6[1];
-  var _useState7 = React.useState(),
-    _useState8 = _slicedToArray__default["default"](_useState7, 2),
-    macromoleculesEditor = _useState8[0],
-    setMacromoleculesEditor = _useState8[1];
-  var _useState9 = React.useState(''),
-    _useState10 = _slicedToArray__default["default"](_useState9, 2),
-    ketcherId = _useState10[0],
-    setKetcherId = _useState10[1];
-  var togglePolymerEditor = function togglePolymerEditor(toggleValue) {
-    setShowPolymerEditor(toggleValue);
-    window.isPolymerEditorTurnedOn = toggleValue;
+  var latest = React.useRef(props);
+  latest.current = props;
+  var current = React.useRef(null);
+  var uiWaiters = React.useRef(new Map());
+  var command = React.useRef(0);
+  var [mode, setMode] = React.useState({ macro: false, command: 0 });
+  var [macroView, setMacroView] = React.useState(null);
+  var [macroError, setMacroError] = React.useState(null);
+  var [sdkId, setSdkId] = React.useState('');
+  React.useLayoutEffect(function () {
+    var resolve = uiWaiters.current.get(mode.command);
+    if (resolve) { uiWaiters.current.delete(mode.command); resolve(); }
+  }, [mode]);
+  var createController = React.useCallback(function () {
+    var controller = new MacroController({
+      deferred: !!latest.current.deferMacromoleculesEditor,
+      load: async function (generation) {
+        setMacroError(null);
+        var module = await nexpolyLoadMacroModule();
+        controller.check();
+        if (generation !== controller.generation) return;
+        setMacroView({ Component: module.default, generation: generation, controller: controller, id: controller.sdk.id });
+      },
+      show: function (value) {
+        return new Promise(function (resolve) {
+          var id = ++command.current;
+          uiWaiters.current.set(id, resolve);
+          setMode({ macro: value, command: id });
+        });
+      },
+      fail: function (error) {
+        if (current.current !== controller) return;
+        setMacroView(null);
+        setMacroError(error);
+      }
+    });
+    current.current = controller;
+    return controller;
+  }, []);
+  var requestMode = function (value) {
+    var controller = current.current;
+    if (!controller) return;
+    controller.requestMode(value).catch(function (error) {
+      if (error.name !== 'AbortError' && controller.state !== 'failed') setMacroError(error);
+    });
   };
-  var togglerComponent = !props.disableMacromoleculesEditor ? jsxRuntime.jsx(ModeControl, {
-    toggle: togglePolymerEditor,
-    isPolymerEditor: showPolymerEditor
+  var toggler = !props.disableMacromoleculesEditor ? React.createElement(ModeControl, {
+    toggle: requestMode, isPolymerEditor: mode.macro
   }) : undefined;
+  var onMicro = async function (sdk) {
+    var controller = sdk.structService.nexpolyMacroController;
+    try {
+      await sdk.structService.info();
+      controller.check();
+      if (!latest.current.deferMacromoleculesEditor && !latest.current.disableMacromoleculesEditor) await controller.ensure();
+      if (sdk.nexpolyInitialMol) await sdk.setMolecule(sdk.nexpolyInitialMol);
+      controller.check();
+      if (!controller.notified) { controller.notified = true; latest.current.onInit?.(sdk); }
+    } catch (error) {
+      if (error.name !== 'AbortError') latest.current.errorHandler?.(String(error));
+    }
+  };
+  var onMacro = function (editor) {
+    var controller = macroView.controller;
+    controller.initialized(editor, macroView.generation);
+  };
   React.useEffect(function () {
-    var switchToMacromoleculesModeHandler = function switchToMacromoleculesModeHandler() {
-      togglePolymerEditor(true);
+    var controller = macroView?.controller;
+    var macro = controller?.macro;
+    if (!macro || macro.disposed) return;
+    var toMacro = function () { requestMode(true); };
+    var toMicro = function () { requestMode(false); };
+    macro.events.switchToMacromoleculesMode.add(toMacro);
+    macro.events.switchToMoleculesMode.add(toMicro);
+    return function () {
+      macro.events.switchToMacromoleculesMode.remove(toMacro);
+      macro.events.switchToMoleculesMode.remove(toMicro);
     };
-    var switchToMoleculesModeHandler = function switchToMoleculesModeHandler() {
-      togglePolymerEditor(false);
-    };
-    if (macromoleculesEditor) {
-      macromoleculesEditor.events.switchToMacromoleculesMode.add(switchToMacromoleculesModeHandler);
-      macromoleculesEditor.events.switchToMoleculesMode.add(switchToMoleculesModeHandler);
-    }
-    return function () {
-      if (macromoleculesEditor) {
-        macromoleculesEditor.events.switchToMacromoleculesMode.remove(switchToMacromoleculesModeHandler);
-        macromoleculesEditor.events.switchToMoleculesMode.remove(switchToMoleculesModeHandler);
-      }
-    };
-  }, [macromoleculesEditor]);
+  }, [macroView, mode]);
   React.useEffect(function () {
     return function () {
+      // Actual SDK retirement owns cancellation; StrictMode's discarded effect
+      // must not retire a controller subsequently created by the live SDK.
+      uiWaiters.current.clear();
       window.isPolymerEditorTurnedOn = false;
     };
   }, []);
-  React.useEffect(function () {
-    if (moleculesEditor && macromoleculesEditor) {
-      if (showPolymerEditor) {
-        macromoleculesEditor === null || macromoleculesEditor === void 0 || macromoleculesEditor.switchToMacromolecules();
-      } else {
-        macromoleculesEditor === null || macromoleculesEditor === void 0 || macromoleculesEditor.switchToMicromolecules();
-        moleculesEditor === null || moleculesEditor === void 0 || moleculesEditor.focusCliparea();
-      }
-    }
-  }, [showPolymerEditor]);
-  React.useEffect(function () {
-    if (ketcher && moleculesEditor && (macromoleculesEditor || props.disableMacromoleculesEditor)) {
-      var _props$onInit;
-      (_props$onInit = props.onInit) === null || _props$onInit === void 0 || _props$onInit.call(props, ketcher);
-    }
-  }, [moleculesEditor, macromoleculesEditor]);
-  var onInitMoleculesEditor = function onInitMoleculesEditor(ketcher) {
-    setKetcher(ketcher);
-    setMoleculesEditor(ketcher.editor);
-  };
-  var onInitMacromoleculesEditor = function onInitMacromoleculesEditor(macromoleculesEditor) {
-    setMacromoleculesEditor(macromoleculesEditor);
-  };
-  return jsxRuntime.jsxs(jsxRuntime.Fragment, {
-    children: [jsxRuntime.jsx("div", {
-      className: styles.editorsWrapper,
-      style: {
-        display: showPolymerEditor ? undefined : 'none'
-      },
-      children: jsxRuntime.jsx(React.Suspense, {
-        fallback: jsxRuntime.jsx("div", {
-          className: styles.switchingLoader,
-          children: jsxRuntime.jsx(LoadingCircles, {})
-        }),
-        children: ketcherId && jsxRuntime.jsx(MacromoleculesEditorComponent, {
-          togglerComponent: togglerComponent,
-          ketcherId: ketcherId,
-          isMacromoleculesEditorTurnedOn: showPolymerEditor,
-          onInit: onInitMacromoleculesEditor
-        })
-      })
-    }), jsxRuntime.jsx("div", {
-      className: styles.editorsWrapper,
-      style: {
-        display: showPolymerEditor ? 'none' : undefined
-      },
-      children: jsxRuntime.jsx(MicromoleculesEditor, _objectSpread(_objectSpread({}, props), {}, {
-        ketcherId: ketcherId,
-        onSetKetcherId: setKetcherId,
-        togglerComponent: togglerComponent,
-        onInit: onInitMoleculesEditor
-      }))
-    })]
-  });
-};
-
-Object.defineProperty(exports, 'EditorClassName', {
-  enumerable: true,
-  get: function () { return ketcherCore.EditorClassName; }
-});
-Object.defineProperty(exports, 'KETCHER_MACROMOLECULES_ROOT_NODE_SELECTOR', {
-  enumerable: true,
-  get: function () { return ketcherCore.KETCHER_MACROMOLECULES_ROOT_NODE_SELECTOR; }
-});
+  var macro = macroView && React.createElement(NexPolyMacroBoundary, {
+    key: macroView.id + ':' + macroView.generation,
+    onError: function (error) { macroView.controller.rejectAttempt(error, macroView.generation); }
+  }, React.createElement(macroView.Component, {
+    ketcherId: macroView.id, togglerComponent: toggler,
+    isMacromoleculesEditorTurnedOn: mode.macro, onInit: onMacro
+  }));
+  return React.createElement(React.Fragment, null,
+    React.createElement('div', { className: styles.editorsWrapper, style: { display: mode.macro ? undefined : 'none' } }, macro),
+    React.createElement('div', { className: styles.editorsWrapper, style: { display: mode.macro ? 'none' : undefined } },
+      React.createElement(MicromoleculesEditor, { ...props, ketcherId: sdkId, onSetKetcherId: setSdkId,
+        createMacroController: createController, togglerComponent: toggler, onInit: onMicro })),
+    macroError && React.createElement('div', { role: 'alert', className: 'np-ketcher-macro-error' },
+      '大分子功能加载失败，当前结构已保留。', React.createElement('button', { type: 'button',
+        onClick: function () { current.current.ensure().then(() => requestMode(true)).catch(() => {}); }
+      }, '重试')));
+};
+
+
 exports.ACS_STYLE_DEFAULT_SETTINGS = ACS_STYLE_DEFAULT_SETTINGS;
 exports.Accordion = Accordion$1;
 exports.AmbiguousMonomerPreview = AmbiguousMonomerPreview;
@@ -37772,4 +38018,3 @@
 exports.ketcherInitEventName = ketcherInitEventName;
 exports.preview = preview;
 exports.usePortalStyle = usePortalStyle;
-//# sourceMappingURL=index.js.map
diff --git a/node_modules/ketcher-react/dist/cjs/index.modern-8e10d5ac.js b/node_modules/ketcher-react/dist/cjs/index.modern-8e10d5ac.js
--- a/node_modules/ketcher-react/dist/cjs/index.modern-8e10d5ac.js
+++ b/node_modules/ketcher-react/dist/cjs/index.modern-8e10d5ac.js
@@ -6010,15 +6010,23 @@
     },
     createEditor: function createEditor(state, action) {
       var _action$payload$onIni, _action$payload;
+      var previousEditor = ketcherCore.CoreEditor.provideEditorInstance();
+      try {
       var editor = new ketcherCore.CoreEditor({
         theme: action.payload.theme,
         canvas: action.payload.canvas,
         monomersLibraryUpdate: action.payload.monomersLibraryUpdate
       });
+      } catch (error) {
+        var partial = ketcherCore.CoreEditor.provideEditorInstance();
+        if (partial && partial !== previousEditor) partial.destroy();
+        throw error;
+      }
       state.editor = editor;
       (_action$payload$onIni = (_action$payload = action.payload).onInit) === null || _action$payload$onIni === void 0 || _action$payload$onIni.call(_action$payload, editor);
     },
-    destroyEditor: function destroyEditor(state) {
+    destroyEditor: function destroyEditor(state, action) {
+      if (state.editor !== action.payload) return;
       var _state$editor, _state$editor2;
       state.editorLayoutMode = (_state$editor = state.editor) === null || _state$editor === void 0 ? void 0 : _state$editor.mode.modeName;
       (_state$editor2 = state.editor) === null || _state$editor2 === void 0 || _state$editor2.destroy();
@@ -14178,13 +14186,19 @@
   }, [editor]);
 }
 
-function useMacromoleculesHotkeys() {
+function useMacromoleculesHotkeys(canvasRef) {
   React.useEffect(function () {
+    var canvas = canvasRef.current;
     var HELP_LINK = process.env.HELP_LINK || 'master';
     var helpUrl = "https://github.com/epam/ketcher/blob/".concat(HELP_LINK, "/documentation/help.md#ketcher-macromolecules-mode");
     var handler = function handler(e) {
       if (e.defaultPrevented) return;
       var target = e.target;
+      var root = canvas?.closest('[data-structure-editor]') || canvas;
+      if (!canvas?.isConnected || !canvas.getClientRects().length || !root?.getClientRects().length ||
+          root.closest('[hidden], [inert], [aria-hidden="true"]') || !(target instanceof Element)) return;
+      var popup = target.closest('[data-ketcher-owner]');
+      if (!root.contains(target) && (!popup || popup.getAttribute('data-ketcher-owner') !== root.getAttribute('data-ketcher-session'))) return;
       var isEditableTarget = !!(target && (target.tagName === 'INPUT' || target.tagName === 'TEXTAREA' || target.isContentEditable || target.closest('[contenteditable="true"]')));
       if (isEditableTarget) return;
       var isQuestionHotkey = e.key === '?' || e.key === '/' && e.shiftKey;
@@ -14468,12 +14482,14 @@
     return isActive(itemKey);
   });
   var activeOption = activeOptions[0];
+  React.useEffect(function () {
   if (subMenuId && activeOption && activeOption !== lastActiveOption) {
     dispatch(setSelectedMenuGroupItem({
       groupName: subMenuId,
       activeItemName: activeOption
     }));
   }
+  }, [dispatch, subMenuId, activeOption, lastActiveOption]);
   var visibleItemId = activeItem !== null && activeItem !== void 0 ? activeItem : activeOption || lastActiveOption || options[0];
   var visibleItem = subComponents.find(function (option) {
     return option.props.itemId === visibleItemId;
@@ -19156,7 +19172,12 @@
   var mergedTheme = _.merge(muiTheme, {
     ketcher: editorTheme
   });
-  store.dispatch(initKetcherId(ketcherId));
+  var nexpolyInitialized = React.useState(null);
+  React.useLayoutEffect(function () {
+    store.dispatch(initKetcherId(ketcherId));
+    nexpolyInitialized[1](ketcherId);
+  }, [ketcherId]);
+  if (nexpolyInitialized[0] !== ketcherId) return null;
   return jsxRuntime.jsx(reactRedux.Provider, {
     store: store,
     children: jsxRuntime.jsxs(react.ThemeProvider, {
@@ -19224,12 +19245,13 @@
       monomersLibraryUpdate: monomersLibraryUpdate,
       onInit: onInit
     }));
+    var ownedEditor = selectEditor(store.getState());
     return function () {
-      dispatch(destroyEditor(null));
+      dispatch(destroyEditor(ownedEditor));
     };
   }, [dispatch]);
   useSetRnaPresets();
-  useMacromoleculesHotkeys();
+  useMacromoleculesHotkeys(canvasRef);
   React.useEffect(function () {
     editor === null || editor === void 0 || editor.events.rightClickSequence.add(function (_ref3) {
       var _ref4 = _slicedToArray__default["default"](_ref3, 2),
@@ -19405,4 +19427,3 @@
 }
 
 exports["default"] = EditorContainer;
-//# sourceMappingURL=index.modern-8e10d5ac.js.map
diff --git a/node_modules/ketcher-react/dist/index.js b/node_modules/ketcher-react/dist/index.js
--- a/node_modules/ketcher-react/dist/index.js
+++ b/node_modules/ketcher-react/dist/index.js
@@ -84,7 +84,13 @@
 function createApi(structServiceProvider, defaultOptions) {
   var _structService$destro;
   var structService = structServiceProvider.createStructService(defaultOptions);
-  var info = structService.info();
+  var info = structService.info().then(function (value) { return value; });
+  Object.defineProperty(info, 'nexpolyMacroController', {
+    get: function () { return structService.nexpolyMacroController; },
+    set: function (controller) { structService.nexpolyMacroController = controller; }
+  });
+  info.catch(function () {});
+  Object.defineProperty(info, "disposed", { get: function () { return structService.disposed; } });
   return Object.assign(info, {
     addKetcherId: structService.addKetcherId.bind(structService),
     info: structService.info.bind(structService),
@@ -456,6 +462,11 @@
         }()
       };
       Object.keys(this.listeners).forEach(function (en) {
+        var handler = _this2.listeners[en];
+        _this2.listeners[en] = function (event) {
+          if (_this2.disposed || !nexpolyOwnsEvent(el.parentElement, event)) return;
+          return handler(event);
+        };
         _this2.target.addEventListener(en, _this2.listeners[en]);
       });
     }
@@ -468,6 +479,8 @@
     key: "componentWillUnmount",
     value: function componentWillUnmount() {
       var _this3 = this;
+      this.disposed = true;
+      this.props.dispose?.();
       Object.keys(this.listeners).forEach(function (en) {
         _this3.target.removeEventListener(en, _this3.listeners[en]);
       });
@@ -3226,7 +3239,10 @@
       _options.rescale;
       _options.fragment;
       var formatterOptions = _objectWithoutProperties(_options, _excluded$B);
-    var format = identifyStructFormat(struct);
+    var controller = server.nexpolyMacroController;
+    var declaredFormat = controller?.detectInputFormat(struct, options['input-format']);
+    var format = controller?.requiresMacroFormat(declaredFormat) ? 'ket' : identifyStructFormat(struct);
+    if (declaredFormat) formatterOptions['input-format'] = declaredFormat;
     if (format === SupportedFormat.cdx) {
       struct = "base64::".concat(struct.replace(/\s/g, ''));
     }
@@ -3258,10 +3274,26 @@
   return selection;
 };
 function load(struct, options) {
+  return function (dispatch, getState) {
+    var controller = getState().server?.nexpolyMacroController;
+    if (!controller) return nexpolyLoadStructure(struct, options)(dispatch, getState);
+    return controller.enqueue(async function () {
+      var result = await nexpolyLoadStructure(struct, options)(dispatch, getState);
+      controller.check();
+      if (controller.macro && controller.macro._type !== (controller.view ? 1 : 0)) await controller.setMode(controller.view);
+      return result;
+    }).catch(function (error) {
+      if (error.name !== 'AbortError') getState().editor.errorHandler?.(String(error));
+    });
+  };
+}
+function nexpolyLoadStructure(struct, options) {
   return function () {
     var _ref = _asyncToGenerator( _regeneratorRuntime.mark(function _callee(dispatch, getState) {
+      var nexpolyController = getState().server?.nexpolyMacroController;
       var state, editor, server, serverSettings, errorHandler, _options2, isPaste, method, otherOptions, parsedStruct, _otherOptions, fragment, hasUnsupportedGroups, hasMoleculeToMonomerConnections, oldStruct, isIndigoFunctionCalled;
       return _regeneratorRuntime.wrap(function _callee$(_context) {
+        nexpolyController?.check();
         while (1) switch (_context.prev = _context.next) {
           case 0:
             state = getState();
@@ -8709,11 +8741,11 @@
   var dialogRef = useRef(null);
   useLayoutEffect(function () {
     if (focusable) {
-      dialogRef.current.focus();
+      nexpolyFocus(dialogRef.current);
     }
     return function () {
       var _dialogRef$current;
-      (_dialogRef$current = dialogRef.current) === null || _dialogRef$current === void 0 || (_dialogRef$current = _dialogRef$current.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)) === null || _dialogRef$current === void 0 || _dialogRef$current.getElementsByClassName(CLIP_AREA_BASE_CLASS)[0].focus();
+      (_dialogRef$current = dialogRef.current) === null || _dialogRef$current === void 0 || (_dialogRef$current = _dialogRef$current.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)) === null || _dialogRef$current === void 0 || _dialogRef$current.getElementsByClassName(CLIP_AREA_BASE_CLASS)[0]?.focus({ preventScroll: true });
     };
   }, [focusable]);
   var isButtonOk = function isButtonOk(button) {
@@ -14814,6 +14846,7 @@
   return function (dispatch, getState) {
     var onResize = throttle(250, function () {
       var state = getState();
+      if (!state.editor || state.editor.disposed) return;
       state.editor.render.update();
       dispatch({
         type: 'CLEAR_VISIBLE',
@@ -14821,6 +14854,7 @@
       });
     });
     addEventListener('resize', onResize);
+    return function () { removeEventListener('resize', onResize); onResize.cancel(); };
   };
 }
 function toolbarReducer () {
@@ -14966,6 +15000,7 @@
 });
 function getRootReducer(setEditor) {
   return function root(state, action) {
+    if (state?.editor?.disposed) return state;
     switch (action.type) {
       case 'INIT':
         {
@@ -15085,25 +15120,46 @@
 function ownKeys$18(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
 function _objectSpread$18(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys$18(Object(t), !0).forEach(function (r) { _defineProperty$1(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$18(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
 var throttleMilliseconds = 100;
-function useThrottleResizeObserver() {
-  var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
-  var _useState = useState({
-      height: undefined,
-      width: undefined
-    }),
-    _useState2 = _slicedToArray$1(_useState, 2),
-    size = _useState2[0],
-    setSize = _useState2[1];
-  var onResize = useMemo(function () {
-    return throttle$1(setSize, throttleMilliseconds);
-  }, []);
-  var _useResizeObserver = useResizeObserver(_objectSpread$18({
-      onResize: onResize
-    }, options)),
-    ref = _useResizeObserver.ref;
-  return _objectSpread$18({
-    ref: ref
-  }, size);
+// A ref callback must not enqueue setState(null) while this nested root unmounts.
+// React Refresh otherwise re-registers the second, empty commit and retains it.
+function nexpolyUseInView(options) {
+  var state = useState(false);
+  var observation = useRef(null);
+  var ref = useCallback(function (element) {
+    observation.current?.disconnect();
+    observation.current = null;
+    if (!element) return;
+    var observer = new IntersectionObserver(function (entries) {
+      if (observation.current !== observer || !element.isConnected) return;
+      var entry = entries[0];
+      state[1](entry.isIntersecting && entry.intersectionRatio >= options.threshold);
+    }, options);
+    observation.current = observer;
+    observer.observe(element);
+  }, [options.threshold]);
+  useLayoutEffect(function () { return function () { ref(null); }; }, [ref]);
+  return [ref, state[0]];
+}
+function useThrottleResizeObserver(options = {}) {
+  var state = useState({ height: undefined, width: undefined });
+  var elementRef = useRef(null);
+  var ref = useCallback(function (element) { elementRef.current = element; }, []);
+  useLayoutEffect(function () {
+    var element = options.ref?.current || elementRef.current;
+    if (!element) return;
+    var active = true;
+    var onResize = throttle$1(function (size) {
+      if (active && element.isConnected && element.getClientRects().length) state[1](size);
+    }, throttleMilliseconds);
+    var observer = new ResizeObserver(function (entries) {
+      if (!active || !element.isConnected || !element.getClientRects().length) return;
+      var rect = entries[0]?.contentRect;
+      if (rect) onResize({ width: Math.round(rect.width), height: Math.round(rect.height) });
+    });
+    observer.observe(element);
+    return function () { active = false; observer.disconnect(); onResize.cancel(); };
+  }, [options.ref]);
+  return { ref: ref, ...state[0] };
 }
 
 function useFormContext() {
@@ -15187,14 +15243,10 @@
   useInterval(scrollBack, isScrollDown ? 100 : null);
   useInterval(scrollForward, isScrollUp ? 100 : null);
   useEffect(function () {
-    return function () {
-      setScrollUp(false);
-    };
+    setScrollUp(false);
   }, [startInView]);
   useEffect(function () {
-    return function () {
-      setScrollDown(false);
-    };
+    setScrollDown(false);
   }, [endInView]);
   return jsxs("div", {
     className: clsx(classes$L.scroll, isLeftRight ? classes$L.leftRight : classes$L.upDown),
@@ -15292,13 +15344,13 @@
     ref = _useResizeObserver.ref,
     height = _useResizeObserver.height;
   var scrollRef = useRef(null);
-  var _useInView = useInView({
+  var _useInView = nexpolyUseInView({
       threshold: 1
     }),
     _useInView2 = _slicedToArray$1(_useInView, 2),
     startRef = _useInView2[0],
     startInView = _useInView2[1];
-  var _useInView3 = useInView({
+  var _useInView3 = nexpolyUseInView({
       threshold: 1
     }),
     _useInView4 = _slicedToArray$1(_useInView3, 2),
@@ -15586,13 +15638,13 @@
   var _useResizeObserver = useThrottleResizeObserver(),
     ref = _useResizeObserver.ref,
     height = _useResizeObserver.height;
-  var _useInView = useInView({
+  var _useInView = nexpolyUseInView({
       threshold: 1
     }),
     _useInView2 = _slicedToArray$1(_useInView, 2),
     startRef = _useInView2[0],
     startInView = _useInView2[1];
-  var _useInView3 = useInView({
+  var _useInView3 = nexpolyUseInView({
       threshold: 1
     }),
     _useInView4 = _slicedToArray$1(_useInView3, 2),
@@ -20166,21 +20218,18 @@
   return _isChangingFunctionalGroup.apply(this, arguments);
 }
 
-var keydownListener = null;
 function initKeydownListener(element) {
   return function (dispatch, getState) {
     var hotKeys = initHotKeys(configWithNonViewOnlyActionsDisabled);
-    keydownListener = function keydownListener(event) {
+    var keydownListener = function keydownListener(event) {
+      if (!nexpolyOwnsEvent(element, event)) return;
       return keyHandle(dispatch, getState, hotKeys, event);
     };
     element.addEventListener('keydown', keydownListener);
-  };
-}
-function removeKeydownListener(element) {
-  return function () {
-    if (keydownListener) {
+    return function () {
       element.removeEventListener('keydown', keydownListener);
-    }
+      clearTimeout(getState().editor?.abbreviationLookupTimeoutId);
+    };
   };
 }
 function removeNotRenderedStruct(actionTool, group, dispatch) {
@@ -20189,7 +20238,6 @@
     dispatch(removeStructAction());
   }
 }
-var abbreviationLookupTimeoutId;
 var ABBREVIATION_LOOKUP_TYPING_TIMEOUT = 1000;
 var shortcutKeys = ['1', '2', '3', '4', 't', 'h', 'n', 'o', 's', 'p', 'f', 'i', 'b', '+', '-'];
 function keyHandle(dispatch, getState, hotKeys, event) {
@@ -20212,16 +20260,16 @@
     var isAbbreviationLookupShown = (!isTheSameKey || !isShortcutKey) && currentlyPressedKeys;
     if (isAbbreviationLookupShown) {
       dispatch(showAbbreviationLookup(event.key));
-      clearTimeout(abbreviationLookupTimeoutId);
-      abbreviationLookupTimeoutId = undefined;
+      clearTimeout(editor.abbreviationLookupTimeoutId);
+      editor.abbreviationLookupTimeoutId = undefined;
       var resetAction = SettingsManager.getSettings().selectionTool;
       dispatch(onAction(resetAction));
       event.preventDefault();
       return;
     } else {
-      abbreviationLookupTimeoutId = window.setTimeout(function () {
+      editor.abbreviationLookupTimeoutId = window.setTimeout(function () {
         dispatch(closeAbbreviationLookup());
-        abbreviationLookupTimeoutId = undefined;
+        editor.abbreviationLookupTimeoutId = undefined;
       }, ABBREVIATION_LOOKUP_TYPING_TIMEOUT);
       dispatch(initAbbreviationLookup(event.key));
     }
@@ -20333,6 +20381,7 @@
   });
   return {
     formats: formats,
+    dispose: function () { debAction.cancel(); loadStruct.cancel(); },
     focused: function focused() {
       var state = getState();
       return !state.modal;
@@ -20586,9 +20635,10 @@
     staticResourcesUrl = _useSettingsContext.staticResourcesUrl;
   useEffect(function () {
     if (typeof requestIdleCallback === 'function') {
-      requestIdleCallback(function () {
+      var idle = requestIdleCallback(function () {
         return onInitTmpls(ref.current, staticResourcesUrl);
       });
+      return function () { cancelIdleCallback(idle); };
     } else {
       onInitTmpls(ref.current, staticResourcesUrl);
     }
@@ -24248,6 +24298,17 @@
     clientArea.addEventListener('mouseleave', this.onMouseLeave);
   }
   _createClass$2(HoverIcon, [{
+    key: "destroy",
+    value: function destroy() {
+      if (!this.editor) return;
+      var area = this.editor.render.clientArea;
+      document.removeEventListener('mousemove', this.onMouseMove);
+      area.removeEventListener('mouseover', this.onMouseMove);
+      area.removeEventListener('mouseleave', this.onMouseLeave);
+      this.element.remove();
+      this.element = this.editor = null;
+    }
+  }, {
     key: "fill",
     get: function get() {
       return this._fill;
@@ -24275,6 +24336,7 @@
   }, {
     key: "onMouseMove",
     value: function onMouseMove(event) {
+      if (!this.editor || this.editor.disposed || !nexpolyOwnsEvent(this.editor.render.clientArea, event)) return;
       if (this.isShown || this.isOverLoader(event) && this.shouldBeShownWhenMouseBack) {
         this.show();
         this.updatePosition();
@@ -25103,6 +25165,23 @@
     this.render.paper.canvas.setAttribute('data-testid', 'canvas');
   }
   _createClass$2(Editor, [{
+    key: "destroy",
+    value: function destroy() {
+      if (this.destroyed) return;
+      this.destroyed = this.disposed = true;
+      this.domDisposers.splice(0).forEach(function (dispose) { dispose(); });
+      clearTimeout(this.abbreviationLookupTimeoutId);
+      clearTimeout(this._tool?.connectedPreviewTimeout);
+      this.hoverIcon.destroy();
+      Object.values(this.event).forEach(function (subscription) { subscription.handlers = []; });
+      this.render.disposed = true;
+      clearTimeout(this.render.updateTimer);
+      this.render.unobserveCanvasResize();
+      this.render.scrollbar.destroy();
+      this.render.paper.remove();
+      this.historyStack = [];
+    }
+  }, {
     key: "isDitrty",
     value: function isDitrty() {
       var position = this.historyPtr;
@@ -25416,6 +25495,20 @@
   }, {
     key: "openMonomerCreationWizard",
     value: function openMonomerCreationWizard() {
+      var owner = ketcherProvider.getKetcher();
+      var controller = owner?.structService.nexpolyMacroController;
+      if (controller && controller.state !== 'ready') {
+        if (this.nexpolyWizardPending) return this.nexpolyWizardPending;
+        var chosen = JSON.stringify(this.selection());
+        var revision = controller.revision;
+        this.nexpolyWizardPending = controller.ensure().then(() => {
+          controller.check();
+          if (!this.disposed && controller.revision === revision && JSON.stringify(this.selection()) === chosen) return this.openMonomerCreationWizard();
+        }).catch(error => { if (error.name !== 'AbortError') this.errorHandler?.(String(error)); })
+          .finally(() => { this.nexpolyWizardPending = null; });
+        return this.nexpolyWizardPending;
+      }
+
       var _selection$atoms2,
         _this2 = this;
       var currentStruct = this.render.ctab.molecule;
@@ -26026,8 +26119,8 @@
   }, {
     key: "focusCliparea",
     value: function focusCliparea() {
-      var cliparea = document.querySelector('.cliparea');
-      cliparea === null || cliparea === void 0 || cliparea.focus();
+      var cliparea = this.render.clientArea.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)?.querySelector('.cliparea');
+      nexpolyFocus(cliparea);
     }
   }], [{
     key: "isSelectionContinuous",
@@ -26129,7 +26222,25 @@
   }
   return false;
 }
+function nexpolyFocus(element) {
+  if (!element || !element.isConnected || !element.getClientRects().length) return;
+  var active = document.activeElement;
+  var root = element.closest('[data-structure-editor]') || element.closest(KETCHER_ROOT_NODE_CSS_SELECTOR);
+  if (active && active !== document.body && !root?.contains(active) &&
+      (active.matches('input,textarea,select') || active.isContentEditable)) return;
+  element.focus({ preventScroll: true });
+}
+function nexpolyOwnsEvent(element, event) {
+  var root = element.closest?.('[data-structure-editor]') || element;
+  if (!element.isConnected || !element.getClientRects().length || !root.isConnected || !root.getClientRects().length || root.closest('[inert], [hidden]')) return false;
+  var target = event.target;
+  if (element.contains(target)) return true;
+  var popup = target?.closest?.('[data-ketcher-owner]');
+  return popup?.getAttribute('data-ketcher-owner') === root.getAttribute('data-ketcher-session');
+}
 function domEventSetup(editor, clientArea) {
+  editor.domDisposers = [];
+  var dragging = false;
   var trackedDomEvents = [{
     target: clientArea,
     eventName: 'click',
@@ -26169,10 +26280,17 @@
       toolEventHandler = _ref13.toolEventHandler;
     editor.event[eventName] = new DOMSubscription();
     var subs = editor.event[eventName];
-    target.addEventListener(eventName, function () {
-      if (window.isPolymerEditorTurnedOn) return;
-      subs.dispatch.apply(subs, arguments);
-    });
+    var callback = function (event) {
+      if (editor.disposed || window.isPolymerEditorTurnedOn) return;
+      var owned = nexpolyOwnsEvent(clientArea, event);
+      if (eventName === 'mousedown' && owned) dragging = true;
+      var finishDrag = eventName === 'mouseup' || eventName === 'mouseleave';
+      if (!owned && !dragging) return;
+      if (finishDrag) dragging = false;
+      subs.dispatch(event);
+    };
+    target.addEventListener(eventName, callback);
+    editor.domDisposers.push(function () { target.removeEventListener(eventName, callback); subs.handlers = []; });
     subs.add(function (event) {
       updateLastCursorPosition(editor, event);
       if (['mouseup', 'mousedown', 'click', 'dbclick'].includes(event.type) && !isMouseMainButtonPressed(event)) {
@@ -29488,7 +29606,7 @@
     value: function componentWillUnmount() {
       removeEditorHandlers(this.editor, this.props);
       this.editorRef.current.removeEventListener('wheel', this.handleWheel);
-      this.editor.render.unobserveCanvasResize();
+      this.editor.destroy();
     }
   }, {
     key: "render",
@@ -33357,7 +33475,8 @@
     setTimeout(function () {
       onOk({
         structStr: structStr,
-        fragment: false
+        fragment: false,
+        'input-format': fileName ? getFormatMimeTypeByFileName(fileName) : undefined
       });
       setIsLoading(false);
     }, 0);
@@ -36898,7 +37017,7 @@
     (_inputRef$current = inputRef.current) === null || _inputRef$current === void 0 || _inputRef$current.focus();
     return function () {
       var _inputRef$current2;
-      (_inputRef$current2 = inputRef.current) === null || _inputRef$current2 === void 0 || (_inputRef$current2 = _inputRef$current2.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)) === null || _inputRef$current2 === void 0 || _inputRef$current2.getElementsByClassName(CLIP_AREA_BASE_CLASS)[0].focus();
+      (_inputRef$current2 = inputRef.current) === null || _inputRef$current2 === void 0 || (_inputRef$current2 = _inputRef$current2.closest(KETCHER_ROOT_NODE_CSS_SELECTOR)) === null || _inputRef$current2 === void 0 || _inputRef$current2.getElementsByClassName(CLIP_AREA_BASE_CLASS)[0]?.focus({ preventScroll: true });
     };
   }, []);
   var closePanel = function closePanel() {
@@ -37101,15 +37220,15 @@
 var handleMouseMove = function handleMouseMove(dispatch, event) {
   dispatch(updateCursorPosition(event.clientX, event.clientY));
 };
-var pointerMoveListener = null;
-var mouseDownListener = null;
 function initMouseListener(element) {
   return function (dispatch, getState) {
     var throttledHandleMouseMove = throttle$1(handleMouseMove, MOUSE_MOVE_THROTTLE_TIMEOUT);
-    pointerMoveListener = function pointerMoveListener(event) {
+    var pointerMoveListener = function pointerMoveListener(event) {
+      if (!nexpolyOwnsEvent(element, event)) return;
       return throttledHandleMouseMove(dispatch, event);
     };
-    mouseDownListener = function mouseDownListener(event) {
+    var mouseDownListener = function mouseDownListener(event) {
+      if (!nexpolyOwnsEvent(element, event)) return;
       var areBothLeftAndRightButtonsClicked = event.buttons === 3;
       if (areBothLeftAndRightButtonsClicked) {
         handleRightClick(getState);
@@ -37117,16 +37236,11 @@
     };
     element.addEventListener('pointermove', pointerMoveListener);
     element.addEventListener('mousedown', mouseDownListener, true);
-  };
-}
-function removeMouseListeners(element) {
-  return function () {
-    if (pointerMoveListener) {
+    return function () {
       element.removeEventListener('pointermove', pointerMoveListener);
-    }
-    if (mouseDownListener) {
-      element.addEventListener('mousedown', mouseDownListener, true);
-    }
+      element.removeEventListener('mousedown', mouseDownListener, true);
+      throttledHandleMouseMove.cancel();
+    };
   };
 }
 function handleRightClick(getState) {
@@ -37152,9 +37266,9 @@
       store.dispatch(setServer(structService));
     };
   };
-  store.dispatch(initKeydownListener(element));
-  store.dispatch(initMouseListener(element));
-  store.dispatch(initResize());
+  var disposeKeys = store.dispatch(initKeydownListener(element));
+  var disposeMouse = store.dispatch(initMouseListener(element));
+  var disposeResize = store.dispatch(initResize());
   appRoot.render(jsx(Provider, {
     store: store,
     children: jsx(settingsContext.Provider, {
@@ -37178,8 +37292,10 @@
     })
   }));
   return function () {
-    store.dispatch(removeKeydownListener(element));
-    store.dispatch(removeMouseListeners(element));
+    disposeKeys();
+    disposeMouse();
+    disposeResize();
+    if (global.currentState?.editor === store.getState().editor) delete global.currentState;
   };
 }
 
@@ -37219,25 +37335,20 @@
   }, {
     key: "reinitializeApi",
     value: function reinitializeApi(ketcherId, structServiceProvider, setStructServiceToStore) {
-      var _this = this;
-      var oldStructService = this.structService;
-      this.structService = createApi(structServiceProvider, DefaultStructServiceOptions);
-      this.structService.addKetcherId(ketcherId);
-      window.addEventListener(STRUCT_SERVICE_INITIALIZED_EVENT, function () {
-        var _oldStructService$des;
-        oldStructService === null || oldStructService === void 0 || (_oldStructService$des = oldStructService.destroy) === null || _oldStructService$des === void 0 || _oldStructService$des.call(oldStructService);
-        if (!_this.structService) {
-          KetcherLogger.warn('Structure service is not reinitialized');
-          return;
-        }
+      var builder = this;
+      var oldService = this.structService;
+      var service = this.structService = createApi(structServiceProvider, DefaultStructServiceOptions);
+      service.addKetcherId(ketcherId);
+      service.info().then(function () {
+        if (builder.disposed || builder.structService !== service) { service.destroy?.(); return; }
         var ketcher = ketcherProvider.getKetcher(ketcherId);
-        ketcher.reinitializeIndigo(_this.structService);
-        IndigoProvider.setIndigo(_this.structService);
-        setStructServiceToStore(_this.structService);
-      }, {
-        once: true
-      });
-      return this.structService;
+        if (!ketcher || ketcher.disposed) { service.destroy?.(); return; }
+        oldService?.destroy?.();
+        ketcher.reinitializeIndigo(service);
+        IndigoProvider.setIndigo(service);
+        setStructServiceToStore(service);
+      }).catch(function (error) { service.destroy?.(); if (!builder.disposed) console.error(error); });
+      return service;
     }
   }, {
     key: "appendServiceMode",
@@ -37307,50 +37418,46 @@
       var params = new URLSearchParams(document.location.search);
       var initialMol = params.get('moll');
       if (initialMol) {
-        ketcher.setMolecule(initialMol);
+        if (ketcher.structService.nexpolyMacroController) ketcher.nexpolyInitialMol = initialMol;
+        else ketcher.setMolecule(initialMol);
       }
       IndigoProvider.setIndigo(this.structService);
       ketcherProvider.addKetcherInstance(ketcher);
+      window.ketcher = ketcher;
       return ketcher;
     }
   }]);
   return KetcherBuilder;
 }();
 
-function buildKetcherAsync(_x) {
-  return _buildKetcherAsync.apply(this, arguments);
-}
-function _buildKetcherAsync() {
-  _buildKetcherAsync = _asyncToGenerator( _regeneratorRuntime.mark(function _callee(_ref) {
-    var element, appRoot, staticResourcesUrl, structServiceProvider, buttons, errorHandler, togglerComponent, customButtons, prevKetcherId, builder, structService, ketcher, _yield$builder$append, cleanup, setServer;
-    return _regeneratorRuntime.wrap(function _callee$(_context) {
-      while (1) switch (_context.prev = _context.next) {
-        case 0:
-          element = _ref.element, appRoot = _ref.appRoot, staticResourcesUrl = _ref.staticResourcesUrl, structServiceProvider = _ref.structServiceProvider, buttons = _ref.buttons, errorHandler = _ref.errorHandler, togglerComponent = _ref.togglerComponent, customButtons = _ref.customButtons, prevKetcherId = _ref.ketcherId;
-          builder = new KetcherBuilder();
-          structService = builder.appendApiAsync(structServiceProvider);
-          builder.appendServiceMode(structServiceProvider.mode);
-          ketcher = builder.build();
-          structService.addKetcherId(ketcher.id);
-          _context.next = 8;
-          return builder.appendUiAsync(prevKetcherId, ketcher.id, element, appRoot, staticResourcesUrl, errorHandler, buttons, togglerComponent, customButtons);
-        case 8:
-          _yield$builder$append = _context.sent;
-          cleanup = _yield$builder$append.cleanup;
-          setServer = _yield$builder$append.setServer;
-          return _context.abrupt("return", {
-            ketcher: ketcher,
-            cleanup: cleanup,
-            builder: builder,
-            setServer: setServer
-          });
-        case 12:
-        case "end":
-          return _context.stop();
-      }
-    }, _callee);
-  }));
-  return _buildKetcherAsync.apply(this, arguments);
+async function buildKetcherAsync(props) {
+  var builder = new KetcherBuilder();
+  var service = builder.appendApiAsync(props.structServiceProvider);
+  builder.appendServiceMode(props.structServiceProvider.mode);
+  if (props.createMacroController) builder.structService.nexpolyMacroController = props.createMacroController();
+  var ketcher = builder.build();
+  service.addKetcherId(ketcher.id);
+  props.onBuild(ketcher, builder);
+  var cleanup;
+  var abort;
+  try {
+    var result = await new Promise(function (resolve, reject) {
+      abort = function () { reject(new DOMException('Editor initialization disposed', 'AbortError')); };
+      props.signal.addEventListener('abort', abort, { once: true });
+      cleanup = initApp(props.ketcherId, ketcher.id, props.element, props.appRoot,
+        props.staticResourcesUrl, { buttons: props.buttons || {}, errorHandler: props.errorHandler || null,
+          version: '3.8.0', buildDate: '2025-10-17T11:21:57', buildNumber: '', customButtons: props.customButtons || [] },
+        service, resolve, props.togglerComponent);
+      props.onCleanup(cleanup);
+      if (props.signal.aborted) abort();
+    });
+    if (props.signal.aborted) throw new DOMException('Editor initialization disposed', 'AbortError');
+    result.editor.errorHandler = typeof props.errorHandler === 'function' ? props.errorHandler : function () {};
+    return { ketcher: ketcher, builder: builder, cleanup: cleanup, setServer: result.setServer };
+  } catch (error) {
+    ketcher.retire();
+    throw error;
+  } finally { props.signal.removeEventListener('abort', abort); }
 }
 
 var styles = {"button-common-styles":"Editor-module_button-common-styles__h4pal","scrollbar":"Editor-module_scrollbar__yVsY8","small":"Editor-module_small__8cgEX","editor":"Editor-module_editor__o0Gjz","-hidden":"Editor-module_-hidden__aKodt","switchingLoader":"Editor-module_switchingLoader__OlmM6","blink":"Editor-module_blink__J7UCV","editorsWrapper":"Editor-module_editorsWrapper__qMSvd"};
@@ -37361,85 +37468,71 @@
   smallWidth: 1040,
   smallHeight: 600
 };
+// Serialize old root cleanup before creating another SDK root, including StrictMode.
+var nexpolyRootCleanup = Promise.resolve();
 function MicromoleculesEditor(props) {
-  var initPromiseRef = useRef(null);
-  var appRootRef = useRef(null);
-  var cleanupRef = useRef(null);
-  var ketcherBuilderRef = useRef(null);
-  var setServerRef = useRef(function () {});
-  var structServiceProvider = props.structServiceProvider;
   var rootElRef = useRef(null);
-  var _useResizeObserver = useThrottleResizeObserver({
-      ref: rootElRef
-    }),
-    height = _useResizeObserver.height,
-    width = _useResizeObserver.width;
+  var active = useRef(null);
+  var latestProps = useRef(props);
+  latestProps.current = props;
+  var dimensions = useThrottleResizeObserver({ ref: rootElRef });
   useEffect(function () {
-    var _ketcherBuilderRef$cu;
-    if (!props.ketcherId) {
-      return;
-    }
-    (_ketcherBuilderRef$cu = ketcherBuilderRef.current) === null || _ketcherBuilderRef$cu === void 0 || _ketcherBuilderRef$cu.reinitializeApi(props.ketcherId, props.structServiceProvider, setServerRef.current);
-  }, [structServiceProvider]);
-  var initKetcher = function () {
-    var _ref = _asyncToGenerator( _regeneratorRuntime.mark(function _callee() {
-      var _initPromiseRef$curre;
-      return _regeneratorRuntime.wrap(function _callee$(_context) {
-        while (1) switch (_context.prev = _context.next) {
-          case 0:
-            appRootRef.current = createRoot(rootElRef.current);
-            initPromiseRef.current = buildKetcherAsync(_objectSpread$1(_objectSpread$1({}, props), {}, {
-              element: rootElRef.current,
-              appRoot: appRootRef.current
-            }));
-            (_initPromiseRef$curre = initPromiseRef.current) === null || _initPromiseRef$curre === void 0 || _initPromiseRef$curre.then(function (_ref2) {
-              var _props$onSetKetcherId;
-              var ketcher = _ref2.ketcher,
-                cleanup = _ref2.cleanup,
-                builder = _ref2.builder,
-                setServer = _ref2.setServer;
-              cleanupRef.current = cleanup;
-              ketcherBuilderRef.current = builder;
-              setServerRef.current = setServer;
-              (_props$onSetKetcherId = props.onSetKetcherId) === null || _props$onSetKetcherId === void 0 || _props$onSetKetcherId.call(props, ketcher.id);
-              if (typeof props.onInit === 'function' && ketcher) {
-                props.onInit(ketcher);
-                var ketcherInitEvent = new Event(ketcherInitEventName(ketcher.id));
-                window.dispatchEvent(ketcherInitEvent);
-              }
-            });
-          case 3:
-          case "end":
-            return _context.stop();
-        }
-      }, _callee);
-    }));
-    return function initKetcher() {
-      return _ref.apply(this, arguments);
-    };
-  }();
+    var current = active.current;
+    if (current?.builder && current.provider !== props.structServiceProvider) {
+      current.provider = props.structServiceProvider;
+      current.builder.reinitializeApi(current.ketcher.id, current.provider, current.setServer);
+    }
+  }, [props.structServiceProvider]);
   useEffect(function () {
-    if (initPromiseRef.current === null) {
-      initKetcher();
-    } else {
-      var _initPromiseRef$curre2;
-      (_initPromiseRef$curre2 = initPromiseRef.current) === null || _initPromiseRef$curre2 === void 0 || _initPromiseRef$curre2["finally"](function () {
-        initKetcher();
-      });
-    }
+    var record = { disposed: false, root: null, cleanup: null, ketcher: null, builder: null,
+      provider: props.structServiceProvider, controller: new AbortController() };
+    active.current = record;
+    var element = rootElRef.current;
+    var start = nexpolyRootCleanup.then(async function () {
+      if (record.disposed) return;
+      var appRoot = { render: function (view) {
+        if (record.disposed) return;
+        record.root = createRoot(element, { onUncaughtError: function (error) {
+          latestProps.current.errorHandler?.(error.message || String(error));
+          record.controller.abort();
+        } });
+        // Create and commit the nested root atomically. An initialization failure
+        // before render never registers an empty root with React Refresh.
+        ReactDOM.flushSync(function () { record.root.render(view); });
+      } };
+      try {
+        var result = await buildKetcherAsync({ ...latestProps.current, element: element, appRoot: appRoot,
+          signal: record.controller.signal,
+          onBuild: function (ketcher, builder) { record.ketcher = ketcher; record.builder = builder; },
+          onCleanup: function (cleanup) { record.cleanup = cleanup; } });
+        if (record.disposed || active.current !== record) { result.ketcher.retire(); return; }
+        record.setServer = result.setServer;
+        latestProps.current.onSetKetcherId?.(result.ketcher.id);
+        latestProps.current.onInit?.(result.ketcher);
+        window.dispatchEvent(new Event(ketcherInitEventName(result.ketcher.id)));
+      } catch (error) {
+        if (!record.disposed) latestProps.current.errorHandler?.(error.message || String(error));
+      }
+    });
     return function () {
-      var _initPromiseRef$curre3;
-      (_initPromiseRef$curre3 = initPromiseRef.current) === null || _initPromiseRef$curre3 === void 0 || _initPromiseRef$curre3.then(function () {
-        var _cleanupRef$current, _appRootRef$current;
-        (_cleanupRef$current = cleanupRef.current) === null || _cleanupRef$current === void 0 || _cleanupRef$current.call(cleanupRef);
-        (_appRootRef$current = appRootRef.current) === null || _appRootRef$current === void 0 || _appRootRef$current.unmount();
+      record.disposed = true;
+      record.controller.abort();
+      record.ketcher?.retire();
+      if (record.builder) { record.builder.disposed = true; record.builder.structService?.destroy?.(); }
+      // Defer nested root unmount until React has finished the host commit.
+      nexpolyRootCleanup = Promise.all([nexpolyRootCleanup, start]).then(function () {
+        record.cleanup?.();
+        record.root?.unmount();
+        if (window.ketcher === record.ketcher) delete window.ketcher;
+        if (IndigoProvider.getIndigo() === record.ketcher?.structService) IndigoProvider.setIndigo(undefined);
+        record.root = record.cleanup = record.builder = record.ketcher = null;
+        if (active.current === record) active.current = null;
       });
     };
   }, []);
-  return jsx("div", {
-    ref: rootElRef,
-    className: clsx(KETCHER_ROOT_NODE_CLASS_NAME, styles.editor, _defineProperty$1({}, styles.small, height && height <= mediaSizes.smallHeight || width && width <= mediaSizes.smallWidth))
-  });
+  return jsx('div', { ref: rootElRef,
+    className: clsx(KETCHER_ROOT_NODE_CLASS_NAME, styles.editor,
+      { [styles.small]: dimensions.height && dimensions.height <= mediaSizes.smallHeight || dimensions.width && dimensions.width <= mediaSizes.smallWidth }) });
 }
 
 var _templateObject, _templateObject2, _templateObject3, _templateObject4, _templateObject5, _templateObject6, _templateObject7, _templateObject8, _templateObject9;
@@ -37554,116 +37647,276 @@
 
 function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
 function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty$1(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
-var MacromoleculesEditorComponent = lazy(function () {
-  return import('./index.modern-c0bcdf82.js');
-});
+const aborted = () => new DOMException('Editor initialization disposed', 'AbortError');
+const macroTypes = new Set(['monomer', 'ambiguousMonomer', 'monomerTemplate',
+  'ambiguousMonomerTemplate', 'monomerGroupTemplate']);
+const macroFormats = new Map([
+  ['helm', 'helm'], ['chemical/x-helm', 'helm'], ['fasta', 'fasta'], ['chemical/x-fasta', 'fasta'],
+  ['sequence', 'sequence'], ['chemical/x-sequence', 'sequence'],
+  ['sequence3Letter', 'sequence-3-letter'], ['sequence-3-letter', 'sequence-3-letter'],
+  ['chemical/x-peptide-sequence-3-letter', 'sequence-3-letter'],
+  ['idt', 'idt'], ['chemical/x-idt', 'idt'],
+  ['chemical/x-rna-sequence', 'sequence'], ['chemical/x-dna-sequence', 'sequence'],
+  ['chemical/x-peptide-sequence', 'sequence']
+]);
+
+function containsMacro(source) {
+  if (typeof source !== 'string' || !source.trim().startsWith('{')) return false;
+  const document = JSON.parse(source);
+  return [...Object.values(document), ...(document.root?.nodes || []), ...(document.root?.templates || [])]
+    .some(value => value && typeof value === 'object' && macroTypes.has(value.type));
+}
+
+/** One controller per SDK, registered before any initial document writes. */
+class MacroController {
+  constructor({ load, show, fail, deferred = true }) {
+    Object.assign(this, { load, show, fail, deferred });
+    this.state = 'idle';
+    this.generation = 0;
+    this.tail = Promise.resolve();
+    this.waiters = new Set();
+    this.view = false;
+  }
+  check() { if (this.state === 'disposed' || this.sdk?.disposed) throw aborted(); }
+  attach(sdk) {
+    this.check(); this.sdk = sdk;
+    this.revision = 0;
+    this.onChange = () => { ++this.revision; };
+    sdk.changeEvent?.add(this.onChange);
+  }
+  requiresMacroFormat(format) { return macroFormats.has(format); }
+  detectInputFormat(source, explicit) {
+    if (explicit) return explicit;
+    if (/^\s*(?:PEPTIDE|RNA|CHEM|BLOB)\d+\s*\{/.test(source)) return 'helm';
+    if (/^\s*>/.test(source)) return 'fasta';
+  }
+  enqueue(action) {
+    try { this.check(); } catch (error) { return Promise.reject(error); }
+    const task = this.tail.then(() => { this.check(); return action(); });
+    this.tail = task.catch(() => {});
+    return this.guard(task);
+  }
+  guard(task) {
+    this.check();
+    return new Promise((resolve, reject) => {
+      const stop = error => reject(error);
+      this.waiters.add(stop);
+      Promise.resolve(task).then(value => { this.check(); resolve(value); }, reject)
+        .catch(reject).finally(() => this.waiters.delete(stop));
+    });
+  }
+  ensure() {
+    try { this.check(); } catch (error) { return Promise.reject(error); }
+    if (this.state === 'ready') return Promise.resolve(this.macro);
+    if (this.pending) return this.pending.promise;
+    const generation = ++this.generation;
+    this.state = 'loading';
+    let resolve, reject;
+    const promise = new Promise((yes, no) => { resolve = yes; reject = no; });
+    promise.catch(() => {});
+    const timer = setTimeout(() => this.rejectAttempt(new Error('Macro initialization timed out'), generation), 15000);
+    this.pending = { promise, resolve, reject, timer, generation };
+    Promise.resolve().then(() => this.load(generation)).catch(error => this.rejectAttempt(error, generation));
+    return promise;
+  }
+  initialized(editor, generation) {
+    if (this.state === 'disposed' || generation !== this.generation || !this.pending) {
+      editor.destroy(); return;
+    }
+    this.macro = editor;
+    // A hidden virgin Macro does not own the small-molecule document.
+    editor._type = 0;
+    this.state = 'ready';
+    const pending = this.pending;
+    this.pending = null;
+    clearTimeout(pending.timer);
+    pending.resolve(editor);
+  }
+  rejectAttempt(error, generation) {
+    if (generation !== this.generation || this.state === 'disposed') return;
+    const pending = this.pending;
+    this.pending = null;
+    this.state = 'failed';
+    if (pending) { clearTimeout(pending.timer); pending.reject(error); }
+    this.fail(error, generation);
+  }
+  async setMode(value) {
+    this.check();
+    const macro = value ? await this.ensure() : this.macro;
+    this.check();
+    if (macro && !macro.disposed && macro._type !== (value ? 1 : 0)) {
+      if (value) macro.switchToMacromolecules();
+      else macro.switchToMicromolecules();
+    }
+    this.view = value;
+    window.isPolymerEditorTurnedOn = value;
+    // The host acknowledges a command id, even if the boolean did not change.
+    await this.guard(this.show(value));
+    if (!value) this.sdk?.editor?.focusCliparea();
+  }
+  requestMode(value) { return this.enqueue(() => this.setMode(value)); }
+  async normalize(source, format, options) {
+    this.check();
+    const explicit = this.detectInputFormat(source, format);
+    if (macroFormats.has(explicit) || containsMacro(source)) await this.ensure();
+    if (macroFormats.has(explicit)) {
+      const sequenceType = /^chemical\/x-(rna|dna|peptide)-sequence$/.exec(explicit)?.[1].toUpperCase();
+      const result = await this.sdk.structService.convert({ struct: source,
+        input_format: macroFormats.get(explicit), output_format: 'chemical/x-indigo-ket' },
+      { ...options, ...(sequenceType ? { 'sequence-type': sequenceType } : {}) });
+      this.check();
+      return { source: result.struct, format: 'ket' };
+    }
+    // Formats that may hide monomers are normalized only during an actual import.
+    if (['mol', 'molV3000', 'cdxml', 'smilesExt', 'chemical/x-mdl-molfile', 'chemical/x-cdxml'].includes(explicit)) {
+      const result = await this.sdk.structService.convert({ struct: source, output_format: 'chemical/x-indigo-ket' });
+      this.check();
+      if (containsMacro(result.struct)) { await this.ensure(); return { source: result.struct, format: 'ket' }; }
+    }
+    return { source, format };
+  }
+  import(source, options, original) {
+    return this.enqueue(async () => {
+      const normalized = await this.normalize(source, options?.inputFormat, options);
+      this.check();
+      const result = await original(normalized.source, options?.inputFormat || normalized.format
+        ? { ...options, inputFormat: normalized.format } : options);
+      this.check();
+      // Importers can change model ownership without changing the visible mode.
+      if (this.macro && this.macro._type !== (this.view ? 1 : 0)) await this.setMode(this.view);
+      return result;
+    });
+  }
+  dispose() {
+    if (this.state === 'disposed') return;
+    ++this.generation;
+    this.state = 'disposed';
+    this.sdk?.changeEvent?.remove(this.onChange);
+    const error = aborted();
+    if (this.pending) { clearTimeout(this.pending.timer); this.pending.reject(error); this.pending = null; }
+    for (const reject of this.waiters) reject(error);
+    this.waiters.clear();
+    // SDK caches can retain an old service wrapper. Retired capabilities must
+    // not keep its document, React callbacks or editor reachable through it.
+    this.sdk = this.macro = this.onChange = undefined;
+    this.load = this.show = this.fail = () => {};
+    this.tail = Promise.resolve();
+  }
+}
+
+function nexpolyLoadMacroModule() { return import("./index.modern-c0bcdf82.js"); }
+// Embedded in both SDK module formats by the audited r4 patch.
+class NexPolyMacroBoundary extends React.Component {
+  constructor(props) { super(props); this.state = { failed: false }; }
+  static getDerivedStateFromError() { return { failed: true }; }
+  componentDidCatch(error) { this.props.onError(error); }
+  render() { return this.state.failed ? null : this.props.children; }
+}
 var Editor = function Editor(props) {
-  var _useState = useState(false),
-    _useState2 = _slicedToArray$1(_useState, 2),
-    showPolymerEditor = _useState2[0],
-    setShowPolymerEditor = _useState2[1];
-  var _useState3 = useState(),
-    _useState4 = _slicedToArray$1(_useState3, 2),
-    moleculesEditor = _useState4[0],
-    setMoleculesEditor = _useState4[1];
-  var _useState5 = useState(),
-    _useState6 = _slicedToArray$1(_useState5, 2),
-    ketcher = _useState6[0],
-    setKetcher = _useState6[1];
-  var _useState7 = useState(),
-    _useState8 = _slicedToArray$1(_useState7, 2),
-    macromoleculesEditor = _useState8[0],
-    setMacromoleculesEditor = _useState8[1];
-  var _useState9 = useState(''),
-    _useState10 = _slicedToArray$1(_useState9, 2),
-    ketcherId = _useState10[0],
-    setKetcherId = _useState10[1];
-  var togglePolymerEditor = function togglePolymerEditor(toggleValue) {
-    setShowPolymerEditor(toggleValue);
-    window.isPolymerEditorTurnedOn = toggleValue;
+  var latest = React.useRef(props);
+  latest.current = props;
+  var current = React.useRef(null);
+  var uiWaiters = React.useRef(new Map());
+  var command = React.useRef(0);
+  var [mode, setMode] = React.useState({ macro: false, command: 0 });
+  var [macroView, setMacroView] = React.useState(null);
+  var [macroError, setMacroError] = React.useState(null);
+  var [sdkId, setSdkId] = React.useState('');
+  React.useLayoutEffect(function () {
+    var resolve = uiWaiters.current.get(mode.command);
+    if (resolve) { uiWaiters.current.delete(mode.command); resolve(); }
+  }, [mode]);
+  var createController = React.useCallback(function () {
+    var controller = new MacroController({
+      deferred: !!latest.current.deferMacromoleculesEditor,
+      load: async function (generation) {
+        setMacroError(null);
+        var module = await nexpolyLoadMacroModule();
+        controller.check();
+        if (generation !== controller.generation) return;
+        setMacroView({ Component: module.default, generation: generation, controller: controller, id: controller.sdk.id });
+      },
+      show: function (value) {
+        return new Promise(function (resolve) {
+          var id = ++command.current;
+          uiWaiters.current.set(id, resolve);
+          setMode({ macro: value, command: id });
+        });
+      },
+      fail: function (error) {
+        if (current.current !== controller) return;
+        setMacroView(null);
+        setMacroError(error);
+      }
+    });
+    current.current = controller;
+    return controller;
+  }, []);
+  var requestMode = function (value) {
+    var controller = current.current;
+    if (!controller) return;
+    controller.requestMode(value).catch(function (error) {
+      if (error.name !== 'AbortError' && controller.state !== 'failed') setMacroError(error);
+    });
   };
-  var togglerComponent = !props.disableMacromoleculesEditor ? jsx(ModeControl, {
-    toggle: togglePolymerEditor,
-    isPolymerEditor: showPolymerEditor
+  var toggler = !props.disableMacromoleculesEditor ? React.createElement(ModeControl, {
+    toggle: requestMode, isPolymerEditor: mode.macro
   }) : undefined;
-  useEffect(function () {
-    var switchToMacromoleculesModeHandler = function switchToMacromoleculesModeHandler() {
-      togglePolymerEditor(true);
+  var onMicro = async function (sdk) {
+    var controller = sdk.structService.nexpolyMacroController;
+    try {
+      await sdk.structService.info();
+      controller.check();
+      if (!latest.current.deferMacromoleculesEditor && !latest.current.disableMacromoleculesEditor) await controller.ensure();
+      if (sdk.nexpolyInitialMol) await sdk.setMolecule(sdk.nexpolyInitialMol);
+      controller.check();
+      if (!controller.notified) { controller.notified = true; latest.current.onInit?.(sdk); }
+    } catch (error) {
+      if (error.name !== 'AbortError') latest.current.errorHandler?.(String(error));
+    }
+  };
+  var onMacro = function (editor) {
+    var controller = macroView.controller;
+    controller.initialized(editor, macroView.generation);
+  };
+  React.useEffect(function () {
+    var controller = macroView?.controller;
+    var macro = controller?.macro;
+    if (!macro || macro.disposed) return;
+    var toMacro = function () { requestMode(true); };
+    var toMicro = function () { requestMode(false); };
+    macro.events.switchToMacromoleculesMode.add(toMacro);
+    macro.events.switchToMoleculesMode.add(toMicro);
+    return function () {
+      macro.events.switchToMacromoleculesMode.remove(toMacro);
+      macro.events.switchToMoleculesMode.remove(toMicro);
     };
-    var switchToMoleculesModeHandler = function switchToMoleculesModeHandler() {
-      togglePolymerEditor(false);
-    };
-    if (macromoleculesEditor) {
-      macromoleculesEditor.events.switchToMacromoleculesMode.add(switchToMacromoleculesModeHandler);
-      macromoleculesEditor.events.switchToMoleculesMode.add(switchToMoleculesModeHandler);
-    }
+  }, [macroView, mode]);
+  React.useEffect(function () {
     return function () {
-      if (macromoleculesEditor) {
-        macromoleculesEditor.events.switchToMacromoleculesMode.remove(switchToMacromoleculesModeHandler);
-        macromoleculesEditor.events.switchToMoleculesMode.remove(switchToMoleculesModeHandler);
-      }
-    };
-  }, [macromoleculesEditor]);
-  useEffect(function () {
-    return function () {
+      // Actual SDK retirement owns cancellation; StrictMode's discarded effect
+      // must not retire a controller subsequently created by the live SDK.
+      uiWaiters.current.clear();
       window.isPolymerEditorTurnedOn = false;
     };
   }, []);
-  useEffect(function () {
-    if (moleculesEditor && macromoleculesEditor) {
-      if (showPolymerEditor) {
-        macromoleculesEditor === null || macromoleculesEditor === void 0 || macromoleculesEditor.switchToMacromolecules();
-      } else {
-        macromoleculesEditor === null || macromoleculesEditor === void 0 || macromoleculesEditor.switchToMicromolecules();
-        moleculesEditor === null || moleculesEditor === void 0 || moleculesEditor.focusCliparea();
-      }
-    }
-  }, [showPolymerEditor]);
-  useEffect(function () {
-    if (ketcher && moleculesEditor && (macromoleculesEditor || props.disableMacromoleculesEditor)) {
-      var _props$onInit;
-      (_props$onInit = props.onInit) === null || _props$onInit === void 0 || _props$onInit.call(props, ketcher);
-    }
-  }, [moleculesEditor, macromoleculesEditor]);
-  var onInitMoleculesEditor = function onInitMoleculesEditor(ketcher) {
-    setKetcher(ketcher);
-    setMoleculesEditor(ketcher.editor);
-  };
-  var onInitMacromoleculesEditor = function onInitMacromoleculesEditor(macromoleculesEditor) {
-    setMacromoleculesEditor(macromoleculesEditor);
-  };
-  return jsxs(Fragment, {
-    children: [jsx("div", {
-      className: styles.editorsWrapper,
-      style: {
-        display: showPolymerEditor ? undefined : 'none'
-      },
-      children: jsx(Suspense, {
-        fallback: jsx("div", {
-          className: styles.switchingLoader,
-          children: jsx(LoadingCircles, {})
-        }),
-        children: ketcherId && jsx(MacromoleculesEditorComponent, {
-          togglerComponent: togglerComponent,
-          ketcherId: ketcherId,
-          isMacromoleculesEditorTurnedOn: showPolymerEditor,
-          onInit: onInitMacromoleculesEditor
-        })
-      })
-    }), jsx("div", {
-      className: styles.editorsWrapper,
-      style: {
-        display: showPolymerEditor ? 'none' : undefined
-      },
-      children: jsx(MicromoleculesEditor, _objectSpread(_objectSpread({}, props), {}, {
-        ketcherId: ketcherId,
-        onSetKetcherId: setKetcherId,
-        togglerComponent: togglerComponent,
-        onInit: onInitMoleculesEditor
-      }))
-    })]
-  });
-};
+  var macro = macroView && React.createElement(NexPolyMacroBoundary, {
+    key: macroView.id + ':' + macroView.generation,
+    onError: function (error) { macroView.controller.rejectAttempt(error, macroView.generation); }
+  }, React.createElement(macroView.Component, {
+    ketcherId: macroView.id, togglerComponent: toggler,
+    isMacromoleculesEditorTurnedOn: mode.macro, onInit: onMacro
+  }));
+  return React.createElement(React.Fragment, null,
+    React.createElement('div', { className: styles.editorsWrapper, style: { display: mode.macro ? undefined : 'none' } }, macro),
+    React.createElement('div', { className: styles.editorsWrapper, style: { display: mode.macro ? 'none' : undefined } },
+      React.createElement(MicromoleculesEditor, { ...props, ketcherId: sdkId, onSetKetcherId: setSdkId,
+        createMacroController: createController, togglerComponent: toggler, onInit: onMicro })),
+    macroError && React.createElement('div', { role: 'alert', className: 'np-ketcher-macro-error' },
+      '大分子功能加载失败，当前结构已保留。', React.createElement('button', { type: 'button',
+        onClick: function () { current.current.ensure().then(() => requestMode(true)).catch(() => {}); }
+      }, '重试')));
+};
+
 
 export { ACS_STYLE_DEFAULT_SETTINGS, Accordion$1 as Accordion, AmbiguousMonomerPreview, appContext as AppContext, ArrowScroll, Button, Container, Content, ContentLine, Dialog, Editor, Header$1 as Header, Icon, IconButton, IconButtonCustomIcon, IndigoProvider, InfoModal$1 as InfoModal, Input$2 as Input, KETCHER_ROOT_NODE_CLASS_NAME, KETCHER_ROOT_NODE_CSS_SELECTOR, MODES, MonomerName, PresetPosition, PreviewType, RatioBar, STRUCT_SERVICE_INITIALIZED_EVENT, STRUCT_SERVICE_NO_RENDER_INITIALIZED_EVENT, STRUCT_TYPE, StructRender, calculateAmbiguousMonomerPreviewLeft, calculateAmbiguousMonomerPreviewTop, calculateBondPreviewPosition, calculateMonomerPreviewTop, calculateNucleoElementPreviewTop, getIconName, ketcherInitEventName, preview, usePortalStyle };
-//# sourceMappingURL=index.js.map
diff --git a/node_modules/ketcher-react/dist/index.modern-c0bcdf82.js b/node_modules/ketcher-react/dist/index.modern-c0bcdf82.js
--- a/node_modules/ketcher-react/dist/index.modern-c0bcdf82.js
+++ b/node_modules/ketcher-react/dist/index.modern-c0bcdf82.js
@@ -5969,15 +5969,23 @@
     },
     createEditor: function createEditor(state, action) {
       var _action$payload$onIni, _action$payload;
+      var previousEditor = CoreEditor.provideEditorInstance();
+      try {
       var editor = new CoreEditor({
         theme: action.payload.theme,
         canvas: action.payload.canvas,
         monomersLibraryUpdate: action.payload.monomersLibraryUpdate
       });
+      } catch (error) {
+        var partial = CoreEditor.provideEditorInstance();
+        if (partial && partial !== previousEditor) partial.destroy();
+        throw error;
+      }
       state.editor = editor;
       (_action$payload$onIni = (_action$payload = action.payload).onInit) === null || _action$payload$onIni === void 0 || _action$payload$onIni.call(_action$payload, editor);
     },
-    destroyEditor: function destroyEditor(state) {
+    destroyEditor: function destroyEditor(state, action) {
+      if (state.editor !== action.payload) return;
       var _state$editor, _state$editor2;
       state.editorLayoutMode = (_state$editor = state.editor) === null || _state$editor === void 0 ? void 0 : _state$editor.mode.modeName;
       (_state$editor2 = state.editor) === null || _state$editor2 === void 0 || _state$editor2.destroy();
@@ -14137,13 +14145,19 @@
   }, [editor]);
 }
 
-function useMacromoleculesHotkeys() {
+function useMacromoleculesHotkeys(canvasRef) {
   useEffect(function () {
+    var canvas = canvasRef.current;
     var HELP_LINK = process.env.HELP_LINK || 'master';
     var helpUrl = "https://github.com/epam/ketcher/blob/".concat(HELP_LINK, "/documentation/help.md#ketcher-macromolecules-mode");
     var handler = function handler(e) {
       if (e.defaultPrevented) return;
       var target = e.target;
+      var root = canvas?.closest('[data-structure-editor]') || canvas;
+      if (!canvas?.isConnected || !canvas.getClientRects().length || !root?.getClientRects().length ||
+          root.closest('[hidden], [inert], [aria-hidden="true"]') || !(target instanceof Element)) return;
+      var popup = target.closest('[data-ketcher-owner]');
+      if (!root.contains(target) && (!popup || popup.getAttribute('data-ketcher-owner') !== root.getAttribute('data-ketcher-session'))) return;
       var isEditableTarget = !!(target && (target.tagName === 'INPUT' || target.tagName === 'TEXTAREA' || target.isContentEditable || target.closest('[contenteditable="true"]')));
       if (isEditableTarget) return;
       var isQuestionHotkey = e.key === '?' || e.key === '/' && e.shiftKey;
@@ -14427,12 +14441,14 @@
     return isActive(itemKey);
   });
   var activeOption = activeOptions[0];
+  React.useEffect(function () {
   if (subMenuId && activeOption && activeOption !== lastActiveOption) {
     dispatch(setSelectedMenuGroupItem({
       groupName: subMenuId,
       activeItemName: activeOption
     }));
   }
+  }, [dispatch, subMenuId, activeOption, lastActiveOption]);
   var visibleItemId = activeItem !== null && activeItem !== void 0 ? activeItem : activeOption || lastActiveOption || options[0];
   var visibleItem = subComponents.find(function (option) {
     return option.props.itemId === visibleItemId;
@@ -19115,7 +19131,12 @@
   var mergedTheme = merge(muiTheme, {
     ketcher: editorTheme
   });
-  store.dispatch(initKetcherId(ketcherId));
+  var nexpolyInitialized = React.useState(null);
+  React.useLayoutEffect(function () {
+    store.dispatch(initKetcherId(ketcherId));
+    nexpolyInitialized[1](ketcherId);
+  }, [ketcherId]);
+  if (nexpolyInitialized[0] !== ketcherId) return null;
   return jsx(Provider, {
     store: store,
     children: jsxs(ThemeProvider, {
@@ -19183,12 +19204,13 @@
       monomersLibraryUpdate: monomersLibraryUpdate,
       onInit: onInit
     }));
+    var ownedEditor = selectEditor(store.getState());
     return function () {
-      dispatch(destroyEditor(null));
+      dispatch(destroyEditor(ownedEditor));
     };
   }, [dispatch]);
   useSetRnaPresets();
-  useMacromoleculesHotkeys();
+  useMacromoleculesHotkeys(canvasRef);
   useEffect(function () {
     editor === null || editor === void 0 || editor.events.rightClickSequence.add(function (_ref3) {
       var _ref4 = _slicedToArray(_ref3, 2),
@@ -19364,4 +19386,3 @@
 }
 
 export { EditorContainer as default };
-//# sourceMappingURL=index.modern-c0bcdf82.js.map
