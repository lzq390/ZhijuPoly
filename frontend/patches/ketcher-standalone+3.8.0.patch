diff --git a/node_modules/ketcher-standalone/dist/cjs/main.js b/node_modules/ketcher-standalone/dist/cjs/main.js
--- a/node_modules/ketcher-standalone/dist/cjs/main.js
+++ b/node_modules/ketcher-standalone/dist/cjs/main.js
@@ -596,3 +596,3 @@
 
-var indigoWorker = new WorkerFactory();
+// NexPoly: Worker ownership is per structure service, never module-global.
 
@@ -738,15 +738,34 @@
     this.defaultOptions = defaultOptions;
-    this.worker = indigoWorker;
-    this.worker.onmessage = function (e) {
-      if (e.data.type === Command.Info) {
-        var callbackMethod = _this.callIndigoLoadedCallback;
-        callbackMethod();
-      }
-      var message = e.data;
-      if (message.type !== undefined) {
-        var event = messageTypeToEventMapping[message.type];
-        _this.EE.emit(event, {
-          data: message
-        });
-      }
+    this.worker = new WorkerFactory();
+    this.queue = [];
+    this.active = null;
+    this.disposed = false;
+    this.worker.onmessage = function (event) {
+      if (_this.disposed) return;
+      var message = event.data;
+      var task = _this.active;
+      if (!task || !message || message.type !== task.message.type ||
+          typeof message.hasError !== 'boolean' ||
+          ((message.type === Command.Convert || message.type === Command.GenerateImageAsBase64) &&
+            message.inputData !== task.message.data.struct)) {
+        _this.destroy(new Error('Invalid Indigo Worker response'));
+        return;
+      }
+      clearTimeout(task.timer);
+      _this.active = null;
+      try {
+        task.action({ data: message });
+      } catch (error) {
+        task.reject(error);
+        _this.destroy(error);
+        return;
+      }
+      _this.drainQueue();
+    };
+    this.worker.onerror = function (event) {
+      event.preventDefault();
+      _this.destroy(new Error(event.message || 'Indigo Worker failed'));
+    };
+    this.worker.onmessageerror = function () {
+      _this.destroy(new Error('Unreadable Indigo Worker response'));
     };
@@ -803,5 +822,3 @@
                 };
-                _this2.EE.removeListener(WorkerEvent.GetInChIKey, action);
-                _this2.EE.addListener(WorkerEvent.GetInChIKey, action);
-                _this2.worker.postMessage(inputMessage);
+                _this2.executeIndigoCallAsync(inputMessage, action, reject);
               }));
@@ -837,7 +854,5 @@
         };
-        _this3.EE.removeListener(WorkerEvent.Info, action);
-        _this3.EE.addListener(WorkerEvent.Info, action);
-        _this3.worker.postMessage({
+        _this3.executeIndigoCallAsync({
           type: Command.Info
-        });
+        }, action, reject);
       });
@@ -890,5 +905,3 @@
         };
-        _this4.EE.removeListener(WorkerEvent.Convert, action);
-        _this4.EE.addListener(WorkerEvent.Convert, action);
-        _this4.worker.postMessage(inputMessage);
+        _this4.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -939,5 +952,3 @@
         };
-        _this5.EE.removeListener(WorkerEvent.Layout, action);
-        _this5.EE.addListener(WorkerEvent.Layout, action);
-        _this5.worker.postMessage(inputMessage);
+        _this5.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -976,5 +987,3 @@
         };
-        _this6.EE.removeListener(WorkerEvent.Clean, action);
-        _this6.EE.addListener(WorkerEvent.Clean, action);
-        _this6.worker.postMessage(inputMessage);
+        _this6.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1011,5 +1020,3 @@
         };
-        _this7.EE.removeListener(WorkerEvent.Aromatize, action);
-        _this7.EE.addListener(WorkerEvent.Aromatize, action);
-        _this7.worker.postMessage(inputMessage);
+        _this7.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1046,5 +1053,3 @@
         };
-        _this8.EE.removeListener(WorkerEvent.Dearomatize, action);
-        _this8.EE.addListener(WorkerEvent.Dearomatize, action);
-        _this8.worker.postMessage(inputMessage);
+        _this8.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1081,5 +1086,3 @@
         };
-        _this9.EE.removeListener(WorkerEvent.CalculateCip, action);
-        _this9.EE.addListener(WorkerEvent.CalculateCip, action);
-        _this9.worker.postMessage(inputMessage);
+        _this9.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1118,5 +1121,3 @@
         };
-        _this10.EE.removeListener(WorkerEvent.Automap, action);
-        _this10.EE.addListener(WorkerEvent.Automap, action);
-        _this10.worker.postMessage(inputMessage);
+        _this10.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1157,5 +1158,3 @@
         };
-        _this11.EE.removeListener(WorkerEvent.Check, action);
-        _this11.EE.addListener(WorkerEvent.Check, action);
-        _this11.worker.postMessage(inputMessage);
+        _this11.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1200,5 +1199,3 @@
         };
-        _this12.EE.removeListener(WorkerEvent.Calculate, action);
-        _this12.EE.addListener(WorkerEvent.Calculate, action);
-        _this12.worker.postMessage(inputMessage);
+        _this12.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1263,5 +1260,3 @@
         };
-        _this13.EE.removeListener(WorkerEvent.GenerateImageAsBase64, action);
-        _this13.EE.addListener(WorkerEvent.GenerateImageAsBase64, action);
-        _this13.worker.postMessage(inputMessage);
+        _this13.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1300,5 +1295,3 @@
         };
-        _this14.EE.removeListener(WorkerEvent.ExplicitHydrogens, action);
-        _this14.EE.addListener(WorkerEvent.ExplicitHydrogens, action);
-        _this14.worker.postMessage(inputMessage);
+        _this14.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1331,5 +1324,3 @@
         };
-        _this15.EE.removeAllListeners(WorkerEvent.CalculateMacromoleculeProperties);
-        _this15.EE.addListener(WorkerEvent.CalculateMacromoleculeProperties, action);
-        _this15.worker.postMessage(inputMessage);
+        _this15.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1337,6 +1328,37 @@
   }, {
+    key: "executeIndigoCallAsync",
+    value: function executeIndigoCallAsync(message, action, reject) {
+      if (this.disposed) { reject(this.failure); return; }
+      this.queue.push({ message: message, action: action, reject: reject });
+      this.drainQueue();
+    }
+  }, {
+    key: "drainQueue",
+    value: function drainQueue() {
+      if (this.disposed || this.active || !this.queue.length) return;
+      var service = this;
+      var task = this.active = this.queue.shift();
+      task.timer = setTimeout(function () {
+        service.destroy(new Error('Indigo Worker request timed out'));
+      }, task.message.type === Command.Info ? 15000 : 8000);
+      try { this.worker.postMessage(task.message); }
+      catch (error) { this.destroy(error); }
+    }
+  }, {
     key: "destroy",
-    value: function destroy() {
+    value: function destroy(error) {
+      if (this.disposed) return;
+      this.disposed = true;
+      this.failure = error || new DOMException('Structure service disposed', 'AbortError');
+      var tasks = this.queue.splice(0);
+      if (this.active) tasks.unshift(this.active);
+      this.active = null;
+      this.worker.onmessage = this.worker.onerror = this.worker.onmessageerror = null;
       this.worker.terminate();
-      this.worker.onmessage = null;
+      tasks.forEach(function (task) {
+        clearTimeout(task.timer);
+        task.reject(this.failure);
+      }, this);
+      this.EE.removeAllListeners();
+      this.ketcherId = null;
     }
@@ -1364,2 +1386 @@
 exports.StandaloneStructServiceProvider = StandaloneStructServiceProvider;
-//# sourceMappingURL=main.js.map
diff --git a/node_modules/ketcher-standalone/dist/main.js b/node_modules/ketcher-standalone/dist/main.js
--- a/node_modules/ketcher-standalone/dist/main.js
+++ b/node_modules/ketcher-standalone/dist/main.js
@@ -582,3 +582,3 @@
 
-var indigoWorker = new WorkerFactory();
+// NexPoly: Worker ownership is per structure service, never module-global.
 
@@ -724,15 +724,34 @@
     this.defaultOptions = defaultOptions;
-    this.worker = indigoWorker;
-    this.worker.onmessage = function (e) {
-      if (e.data.type === Command.Info) {
-        var callbackMethod = _this.callIndigoLoadedCallback;
-        callbackMethod();
-      }
-      var message = e.data;
-      if (message.type !== undefined) {
-        var event = messageTypeToEventMapping[message.type];
-        _this.EE.emit(event, {
-          data: message
-        });
-      }
+    this.worker = new WorkerFactory();
+    this.queue = [];
+    this.active = null;
+    this.disposed = false;
+    this.worker.onmessage = function (event) {
+      if (_this.disposed) return;
+      var message = event.data;
+      var task = _this.active;
+      if (!task || !message || message.type !== task.message.type ||
+          typeof message.hasError !== 'boolean' ||
+          ((message.type === Command.Convert || message.type === Command.GenerateImageAsBase64) &&
+            message.inputData !== task.message.data.struct)) {
+        _this.destroy(new Error('Invalid Indigo Worker response'));
+        return;
+      }
+      clearTimeout(task.timer);
+      _this.active = null;
+      try {
+        task.action({ data: message });
+      } catch (error) {
+        task.reject(error);
+        _this.destroy(error);
+        return;
+      }
+      _this.drainQueue();
+    };
+    this.worker.onerror = function (event) {
+      event.preventDefault();
+      _this.destroy(new Error(event.message || 'Indigo Worker failed'));
+    };
+    this.worker.onmessageerror = function () {
+      _this.destroy(new Error('Unreadable Indigo Worker response'));
     };
@@ -789,5 +808,3 @@
                 };
-                _this2.EE.removeListener(WorkerEvent.GetInChIKey, action);
-                _this2.EE.addListener(WorkerEvent.GetInChIKey, action);
-                _this2.worker.postMessage(inputMessage);
+                _this2.executeIndigoCallAsync(inputMessage, action, reject);
               }));
@@ -823,7 +840,5 @@
         };
-        _this3.EE.removeListener(WorkerEvent.Info, action);
-        _this3.EE.addListener(WorkerEvent.Info, action);
-        _this3.worker.postMessage({
+        _this3.executeIndigoCallAsync({
           type: Command.Info
-        });
+        }, action, reject);
       });
@@ -876,5 +891,3 @@
         };
-        _this4.EE.removeListener(WorkerEvent.Convert, action);
-        _this4.EE.addListener(WorkerEvent.Convert, action);
-        _this4.worker.postMessage(inputMessage);
+        _this4.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -925,5 +938,3 @@
         };
-        _this5.EE.removeListener(WorkerEvent.Layout, action);
-        _this5.EE.addListener(WorkerEvent.Layout, action);
-        _this5.worker.postMessage(inputMessage);
+        _this5.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -962,5 +973,3 @@
         };
-        _this6.EE.removeListener(WorkerEvent.Clean, action);
-        _this6.EE.addListener(WorkerEvent.Clean, action);
-        _this6.worker.postMessage(inputMessage);
+        _this6.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -997,5 +1006,3 @@
         };
-        _this7.EE.removeListener(WorkerEvent.Aromatize, action);
-        _this7.EE.addListener(WorkerEvent.Aromatize, action);
-        _this7.worker.postMessage(inputMessage);
+        _this7.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1032,5 +1039,3 @@
         };
-        _this8.EE.removeListener(WorkerEvent.Dearomatize, action);
-        _this8.EE.addListener(WorkerEvent.Dearomatize, action);
-        _this8.worker.postMessage(inputMessage);
+        _this8.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1067,5 +1072,3 @@
         };
-        _this9.EE.removeListener(WorkerEvent.CalculateCip, action);
-        _this9.EE.addListener(WorkerEvent.CalculateCip, action);
-        _this9.worker.postMessage(inputMessage);
+        _this9.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1104,5 +1107,3 @@
         };
-        _this10.EE.removeListener(WorkerEvent.Automap, action);
-        _this10.EE.addListener(WorkerEvent.Automap, action);
-        _this10.worker.postMessage(inputMessage);
+        _this10.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1143,5 +1144,3 @@
         };
-        _this11.EE.removeListener(WorkerEvent.Check, action);
-        _this11.EE.addListener(WorkerEvent.Check, action);
-        _this11.worker.postMessage(inputMessage);
+        _this11.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1186,5 +1185,3 @@
         };
-        _this12.EE.removeListener(WorkerEvent.Calculate, action);
-        _this12.EE.addListener(WorkerEvent.Calculate, action);
-        _this12.worker.postMessage(inputMessage);
+        _this12.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1249,5 +1246,3 @@
         };
-        _this13.EE.removeListener(WorkerEvent.GenerateImageAsBase64, action);
-        _this13.EE.addListener(WorkerEvent.GenerateImageAsBase64, action);
-        _this13.worker.postMessage(inputMessage);
+        _this13.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1286,5 +1281,3 @@
         };
-        _this14.EE.removeListener(WorkerEvent.ExplicitHydrogens, action);
-        _this14.EE.addListener(WorkerEvent.ExplicitHydrogens, action);
-        _this14.worker.postMessage(inputMessage);
+        _this14.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1317,5 +1310,3 @@
         };
-        _this15.EE.removeAllListeners(WorkerEvent.CalculateMacromoleculeProperties);
-        _this15.EE.addListener(WorkerEvent.CalculateMacromoleculeProperties, action);
-        _this15.worker.postMessage(inputMessage);
+        _this15.executeIndigoCallAsync(inputMessage, action, reject);
       });
@@ -1323,6 +1314,37 @@
   }, {
+    key: "executeIndigoCallAsync",
+    value: function executeIndigoCallAsync(message, action, reject) {
+      if (this.disposed) { reject(this.failure); return; }
+      this.queue.push({ message: message, action: action, reject: reject });
+      this.drainQueue();
+    }
+  }, {
+    key: "drainQueue",
+    value: function drainQueue() {
+      if (this.disposed || this.active || !this.queue.length) return;
+      var service = this;
+      var task = this.active = this.queue.shift();
+      task.timer = setTimeout(function () {
+        service.destroy(new Error('Indigo Worker request timed out'));
+      }, task.message.type === Command.Info ? 15000 : 8000);
+      try { this.worker.postMessage(task.message); }
+      catch (error) { this.destroy(error); }
+    }
+  }, {
     key: "destroy",
-    value: function destroy() {
+    value: function destroy(error) {
+      if (this.disposed) return;
+      this.disposed = true;
+      this.failure = error || new DOMException('Structure service disposed', 'AbortError');
+      var tasks = this.queue.splice(0);
+      if (this.active) tasks.unshift(this.active);
+      this.active = null;
+      this.worker.onmessage = this.worker.onerror = this.worker.onmessageerror = null;
       this.worker.terminate();
-      this.worker.onmessage = null;
+      tasks.forEach(function (task) {
+        clearTimeout(task.timer);
+        task.reject(this.failure);
+      }, this);
+      this.EE.removeAllListeners();
+      this.ketcherId = null;
     }
@@ -1347,2 +1369 @@
 export { STRUCT_SERVICE_INITIALIZED_EVENT, STRUCT_SERVICE_NO_RENDER_INITIALIZED_EVENT, IndigoService as StandaloneStructService, StandaloneStructServiceProvider };
-//# sourceMappingURL=main.js.map
diff --git a/node_modules/ketcher-standalone/package.json b/node_modules/ketcher-standalone/package.json
--- a/node_modules/ketcher-standalone/package.json
+++ b/node_modules/ketcher-standalone/package.json
@@ -82,2 +82,3 @@
     ".": {
+      "types": "./dist/index.d.ts",
       "import": "./dist/main.js",
