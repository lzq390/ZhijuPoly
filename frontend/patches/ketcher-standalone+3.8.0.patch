diff --git a/node_modules/ketcher-standalone/dist/cjs/infrastructure/services/struct/standaloneStructService.d.ts b/node_modules/ketcher-standalone/dist/cjs/infrastructure/services/struct/standaloneStructService.d.ts
--- a/node_modules/ketcher-standalone/dist/cjs/infrastructure/services/struct/standaloneStructService.d.ts
+++ b/node_modules/ketcher-standalone/dist/cjs/infrastructure/services/struct/standaloneStructService.d.ts
@@ -15,4 +15,13 @@
  ***************************************************************************/
 import { AromatizeData, AromatizeResult, AutomapData, AutomapResult, CalculateCipData, CalculateCipResult, CalculateData, CalculateResult, CheckData, CheckResult, CleanData, CleanResult, ConvertData, ConvertResult, DearomatizeData, DearomatizeResult, ExplicitHydrogensData, ExplicitHydrogensResult, GenerateImageOptions, InfoResult, LayoutData, LayoutResult, RecognizeResult, StructService, StructServiceOptions, CalculateMacromoleculePropertiesData, CalculateMacromoleculePropertiesResult } from 'ketcher-core';
+/** A live protocol transport. Ownership transfers to this service. */
+interface OwnedIndigoTransport {
+    readonly worker: Worker;
+    readonly disposed: boolean;
+    readonly failure?: unknown;
+    info(): Promise<InfoResult>;
+    dispatch(message: unknown, action: (event: { data: any }) => void, reject: (error: unknown) => void): void;
+    destroy(error?: unknown): void;
+}
 declare class IndigoService implements StructService {
     private readonly defaultOptions;
@@ -20,5 +29,5 @@
     private readonly EE;
     private ketcherId;
-    constructor(defaultOptions: StructServiceOptions);
+    constructor(defaultOptions: StructServiceOptions, transport?: OwnedIndigoTransport);
     addKetcherId(ketcherId: string): void;
     private getStandardServerOptions;
diff --git a/node_modules/ketcher-standalone/dist/cjs/main.js b/node_modules/ketcher-standalone/dist/cjs/main.js
--- a/node_modules/ketcher-standalone/dist/cjs/main.js
+++ b/node_modules/ketcher-standalone/dist/cjs/main.js
@@ -595,5 +595,5 @@
 /* eslint-enable */
 
-var indigoWorker = new WorkerFactory();
+// NexPoly: Worker ownership is per structure service, never module-global.
 
 var _excluded = ["outputFormat", "backgroundColor"];
@@ -730,6 +730,79 @@
 }
 var messageTypeToEventMapping = (_messageTypeToEventMa = {}, _defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_messageTypeToEventMa, Command.Info, WorkerEvent.Info), Command.Convert, WorkerEvent.Convert), Command.Layout, WorkerEvent.Layout), Command.Clean, WorkerEvent.Clean), Command.Aromatize, WorkerEvent.Aromatize), Command.Dearomatize, WorkerEvent.Dearomatize), Command.CalculateCip, WorkerEvent.CalculateCip), Command.Automap, WorkerEvent.Automap), Command.Check, WorkerEvent.Check), Command.Calculate, WorkerEvent.Calculate), _defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_defineProperty__default["default"](_messageTypeToEventMa, Command.GenerateImageAsBase64, WorkerEvent.GenerateImageAsBase64), Command.GetInChIKey, WorkerEvent.GetInChIKey), Command.ExplicitHydrogens, WorkerEvent.ExplicitHydrogens), Command.CalculateMacromoleculeProperties, WorkerEvent.CalculateMacromoleculeProperties));
+/** Protocol-compatible r3 queue, independent of React and ketcher-core. */
+class NexPolyIndigoTransport {
+  constructor(worker) {
+    this.worker = worker;
+    this.queue = [];
+    this.active = null;
+    this.disposed = false;
+    worker.onmessage = event => {
+      if (this.disposed) return;
+      const message = event.data;
+      const task = this.active;
+      if (!task || !message || message.type !== task.message.type ||
+          typeof message.hasError !== 'boolean' ||
+          ([1, 10].includes(message.type) && message.inputData !== task.message.data.struct)) {
+        this.destroy(new Error('Invalid Indigo Worker response'));
+        return;
+      }
+      clearTimeout(task.timer);
+      this.active = null;
+      try { task.action({ data: message }); }
+      catch (error) { task.reject(error); this.destroy(error); return; }
+      this.drainQueue();
+    };
+    worker.onerror = event => {
+      event.preventDefault();
+      this.destroy(new Error(event.message || 'Indigo Worker failed'));
+    };
+    worker.onmessageerror = () => this.destroy(new Error('Unreadable Indigo Worker response'));
+  }
+
+  dispatch(message, action, reject) {
+    if (this.disposed) { reject(this.failure); return; }
+    this.queue.push({ message, action, reject });
+    this.drainQueue();
+  }
+
+  info() {
+    if (this.disposed) return Promise.reject(this.failure);
+    if (!this.infoPromise) {
+      this.infoPromise = new Promise((resolve, reject) => {
+        this.dispatch({ type: 0 }, ({ data }) => {
+          if (data.hasError) reject(new Error(String(data.error)));
+          else resolve({ indigoVersion: data.payload, imagoVersions: [], isAvailable: true });
+        }, reject);
+      });
+      // Preheating has no consumer until a visible session takes ownership.
+      this.infoPromise.catch(() => {});
+    }
+    return this.infoPromise;
+  }
+
+  drainQueue() {
+    if (this.disposed || this.active || !this.queue.length) return;
+    const task = this.active = this.queue.shift();
+    task.timer = setTimeout(() => this.destroy(new Error('Indigo Worker request timed out')),
+      task.message.type === 0 ? 15000 : 8000);
+    try { this.worker.postMessage(task.message); }
+    catch (error) { this.destroy(error); }
+  }
+
+  destroy(error = new DOMException('Structure service disposed', 'AbortError')) {
+    if (this.disposed) return;
+    this.disposed = true;
+    this.failure = error;
+    const tasks = this.queue.splice(0);
+    if (this.active) tasks.unshift(this.active);
+    this.active = null;
+    this.worker.onmessage = this.worker.onerror = this.worker.onmessageerror = null;
+    this.worker.terminate();
+    for (const task of tasks) { clearTimeout(task.timer); task.reject(error); }
+  }
+}
+
 var IndigoService = function () {
-  function IndigoService(defaultOptions) {
+  function IndigoService(defaultOptions, transport) {
     var _this = this;
     _classCallCheck__default["default"](this, IndigoService);
@@ -737,18 +810,8 @@
     _defineProperty__default["default"](this, "ketcherId", null);
     this.defaultOptions = defaultOptions;
-    this.worker = indigoWorker;
-    this.worker.onmessage = function (e) {
-      if (e.data.type === Command.Info) {
-        var callbackMethod = _this.callIndigoLoadedCallback;
-        callbackMethod();
-      }
-      var message = e.data;
-      if (message.type !== undefined) {
-        var event = messageTypeToEventMapping[message.type];
-        _this.EE.emit(event, {
-          data: message
-        });
-      }
-    };
+    this.transport = transport || new NexPolyIndigoTransport(new WorkerFactory());
+    this.worker = this.transport.worker;
+    Object.defineProperty(this, 'disposed', { get: () => this.transport.disposed });
+    Object.defineProperty(this, 'failure', { get: () => this.transport.failure });
   }
   _createClass__default["default"](IndigoService, [{
@@ -802,7 +865,5 @@
                   }
                 };
-                _this2.EE.removeListener(WorkerEvent.GetInChIKey, action);
-                _this2.EE.addListener(WorkerEvent.GetInChIKey, action);
-                _this2.worker.postMessage(inputMessage);
+                _this2.executeIndigoCallAsync(inputMessage, action, reject);
               }));
             case 1:
@@ -819,28 +880,5 @@
   }, {
     key: "info",
-    value: function info() {
-      var _this3 = this;
-      return new Promise(function (resolve, reject) {
-        var action = function action(_ref2) {
-          var data = _ref2.data;
-          var msg = data;
-          if (!msg.hasError) {
-            var result = {
-              indigoVersion: msg.payload,
-              imagoVersions: [],
-              isAvailable: true
-            };
-            resolve(result);
-          } else {
-            reject(msg.error);
-          }
-        };
-        _this3.EE.removeListener(WorkerEvent.Info, action);
-        _this3.EE.addListener(WorkerEvent.Info, action);
-        _this3.worker.postMessage({
-          type: Command.Info
-        });
-      });
-    }
+    value: function info() { return this.transport.info(); }
   }, {
     key: "convert",
@@ -889,7 +927,5 @@
           data: commandData
         };
-        _this4.EE.removeListener(WorkerEvent.Convert, action);
-        _this4.EE.addListener(WorkerEvent.Convert, action);
-        _this4.worker.postMessage(inputMessage);
+        _this4.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -938,7 +974,5 @@
           data: commandData
         };
-        _this5.EE.removeListener(WorkerEvent.Layout, action);
-        _this5.EE.addListener(WorkerEvent.Layout, action);
-        _this5.worker.postMessage(inputMessage);
+        _this5.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -975,7 +1009,5 @@
           data: commandData
         };
-        _this6.EE.removeListener(WorkerEvent.Clean, action);
-        _this6.EE.addListener(WorkerEvent.Clean, action);
-        _this6.worker.postMessage(inputMessage);
+        _this6.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1010,7 +1042,5 @@
           data: commandData
         };
-        _this7.EE.removeListener(WorkerEvent.Aromatize, action);
-        _this7.EE.addListener(WorkerEvent.Aromatize, action);
-        _this7.worker.postMessage(inputMessage);
+        _this7.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1045,7 +1075,5 @@
           data: commandData
         };
-        _this8.EE.removeListener(WorkerEvent.Dearomatize, action);
-        _this8.EE.addListener(WorkerEvent.Dearomatize, action);
-        _this8.worker.postMessage(inputMessage);
+        _this8.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1080,7 +1108,5 @@
           data: commandData
         };
-        _this9.EE.removeListener(WorkerEvent.CalculateCip, action);
-        _this9.EE.addListener(WorkerEvent.CalculateCip, action);
-        _this9.worker.postMessage(inputMessage);
+        _this9.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1117,7 +1143,5 @@
           data: commandData
         };
-        _this10.EE.removeListener(WorkerEvent.Automap, action);
-        _this10.EE.addListener(WorkerEvent.Automap, action);
-        _this10.worker.postMessage(inputMessage);
+        _this10.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1156,7 +1180,5 @@
           data: commandData
         };
-        _this11.EE.removeListener(WorkerEvent.Check, action);
-        _this11.EE.addListener(WorkerEvent.Check, action);
-        _this11.worker.postMessage(inputMessage);
+        _this11.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1199,7 +1221,5 @@
           data: commandData
         };
-        _this12.EE.removeListener(WorkerEvent.Calculate, action);
-        _this12.EE.addListener(WorkerEvent.Calculate, action);
-        _this12.worker.postMessage(inputMessage);
+        _this12.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1262,7 +1282,5 @@
           data: commandData
         };
-        _this13.EE.removeListener(WorkerEvent.GenerateImageAsBase64, action);
-        _this13.EE.addListener(WorkerEvent.GenerateImageAsBase64, action);
-        _this13.worker.postMessage(inputMessage);
+        _this13.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1299,7 +1317,5 @@
           data: commandData
         };
-        _this14.EE.removeListener(WorkerEvent.ExplicitHydrogens, action);
-        _this14.EE.addListener(WorkerEvent.ExplicitHydrogens, action);
-        _this14.worker.postMessage(inputMessage);
+        _this14.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1330,14 +1346,22 @@
           data: commandData
         };
-        _this15.EE.removeAllListeners(WorkerEvent.CalculateMacromoleculeProperties);
-        _this15.EE.addListener(WorkerEvent.CalculateMacromoleculeProperties, action);
-        _this15.worker.postMessage(inputMessage);
+        _this15.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
   }, {
+    key: "executeIndigoCallAsync",
+    value: function executeIndigoCallAsync(message, action, reject) {
+      this.transport.dispatch(message, action, reject);
+    }
+  }, {
     key: "destroy",
-    value: function destroy() {
-      this.worker.terminate();
-      this.worker.onmessage = null;
+    value: function destroy(error) {
+      if (this.disposed) return;
+      this.transport.destroy(error);
+      // Retirement can happen before onInit gives the SDK to the host.
+      this.nexpolyMacroController?.sdk?.retire();
+      this.nexpolyMacroController?.dispose();
+      this.EE.removeAllListeners();
+      this.ketcherId = null;
     }
   }]);
@@ -1363,3 +1387,2 @@
 exports.StandaloneStructService = IndigoService;
 exports.StandaloneStructServiceProvider = StandaloneStructServiceProvider;
-//# sourceMappingURL=main.js.map
diff --git a/node_modules/ketcher-standalone/dist/infrastructure/services/struct/standaloneStructService.d.ts b/node_modules/ketcher-standalone/dist/infrastructure/services/struct/standaloneStructService.d.ts
--- a/node_modules/ketcher-standalone/dist/infrastructure/services/struct/standaloneStructService.d.ts
+++ b/node_modules/ketcher-standalone/dist/infrastructure/services/struct/standaloneStructService.d.ts
@@ -15,4 +15,13 @@
  ***************************************************************************/
 import { AromatizeData, AromatizeResult, AutomapData, AutomapResult, CalculateCipData, CalculateCipResult, CalculateData, CalculateResult, CheckData, CheckResult, CleanData, CleanResult, ConvertData, ConvertResult, DearomatizeData, DearomatizeResult, ExplicitHydrogensData, ExplicitHydrogensResult, GenerateImageOptions, InfoResult, LayoutData, LayoutResult, RecognizeResult, StructService, StructServiceOptions, CalculateMacromoleculePropertiesData, CalculateMacromoleculePropertiesResult } from 'ketcher-core';
+/** A live protocol transport. Ownership transfers to this service. */
+interface OwnedIndigoTransport {
+    readonly worker: Worker;
+    readonly disposed: boolean;
+    readonly failure?: unknown;
+    info(): Promise<InfoResult>;
+    dispatch(message: unknown, action: (event: { data: any }) => void, reject: (error: unknown) => void): void;
+    destroy(error?: unknown): void;
+}
 declare class IndigoService implements StructService {
     private readonly defaultOptions;
@@ -20,5 +29,5 @@
     private readonly EE;
     private ketcherId;
-    constructor(defaultOptions: StructServiceOptions);
+    constructor(defaultOptions: StructServiceOptions, transport?: OwnedIndigoTransport);
     addKetcherId(ketcherId: string): void;
     private getStandardServerOptions;
diff --git a/node_modules/ketcher-standalone/dist/main.js b/node_modules/ketcher-standalone/dist/main.js
--- a/node_modules/ketcher-standalone/dist/main.js
+++ b/node_modules/ketcher-standalone/dist/main.js
@@ -581,5 +581,5 @@
 /* eslint-enable */
 
-var indigoWorker = new WorkerFactory();
+// NexPoly: Worker ownership is per structure service, never module-global.
 
 var _excluded = ["outputFormat", "backgroundColor"];
@@ -716,6 +716,79 @@
 }
 var messageTypeToEventMapping = (_messageTypeToEventMa = {}, _defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty(_messageTypeToEventMa, Command.Info, WorkerEvent.Info), Command.Convert, WorkerEvent.Convert), Command.Layout, WorkerEvent.Layout), Command.Clean, WorkerEvent.Clean), Command.Aromatize, WorkerEvent.Aromatize), Command.Dearomatize, WorkerEvent.Dearomatize), Command.CalculateCip, WorkerEvent.CalculateCip), Command.Automap, WorkerEvent.Automap), Command.Check, WorkerEvent.Check), Command.Calculate, WorkerEvent.Calculate), _defineProperty(_defineProperty(_defineProperty(_defineProperty(_messageTypeToEventMa, Command.GenerateImageAsBase64, WorkerEvent.GenerateImageAsBase64), Command.GetInChIKey, WorkerEvent.GetInChIKey), Command.ExplicitHydrogens, WorkerEvent.ExplicitHydrogens), Command.CalculateMacromoleculeProperties, WorkerEvent.CalculateMacromoleculeProperties));
+/** Protocol-compatible r3 queue, independent of React and ketcher-core. */
+class NexPolyIndigoTransport {
+  constructor(worker) {
+    this.worker = worker;
+    this.queue = [];
+    this.active = null;
+    this.disposed = false;
+    worker.onmessage = event => {
+      if (this.disposed) return;
+      const message = event.data;
+      const task = this.active;
+      if (!task || !message || message.type !== task.message.type ||
+          typeof message.hasError !== 'boolean' ||
+          ([1, 10].includes(message.type) && message.inputData !== task.message.data.struct)) {
+        this.destroy(new Error('Invalid Indigo Worker response'));
+        return;
+      }
+      clearTimeout(task.timer);
+      this.active = null;
+      try { task.action({ data: message }); }
+      catch (error) { task.reject(error); this.destroy(error); return; }
+      this.drainQueue();
+    };
+    worker.onerror = event => {
+      event.preventDefault();
+      this.destroy(new Error(event.message || 'Indigo Worker failed'));
+    };
+    worker.onmessageerror = () => this.destroy(new Error('Unreadable Indigo Worker response'));
+  }
+
+  dispatch(message, action, reject) {
+    if (this.disposed) { reject(this.failure); return; }
+    this.queue.push({ message, action, reject });
+    this.drainQueue();
+  }
+
+  info() {
+    if (this.disposed) return Promise.reject(this.failure);
+    if (!this.infoPromise) {
+      this.infoPromise = new Promise((resolve, reject) => {
+        this.dispatch({ type: 0 }, ({ data }) => {
+          if (data.hasError) reject(new Error(String(data.error)));
+          else resolve({ indigoVersion: data.payload, imagoVersions: [], isAvailable: true });
+        }, reject);
+      });
+      // Preheating has no consumer until a visible session takes ownership.
+      this.infoPromise.catch(() => {});
+    }
+    return this.infoPromise;
+  }
+
+  drainQueue() {
+    if (this.disposed || this.active || !this.queue.length) return;
+    const task = this.active = this.queue.shift();
+    task.timer = setTimeout(() => this.destroy(new Error('Indigo Worker request timed out')),
+      task.message.type === 0 ? 15000 : 8000);
+    try { this.worker.postMessage(task.message); }
+    catch (error) { this.destroy(error); }
+  }
+
+  destroy(error = new DOMException('Structure service disposed', 'AbortError')) {
+    if (this.disposed) return;
+    this.disposed = true;
+    this.failure = error;
+    const tasks = this.queue.splice(0);
+    if (this.active) tasks.unshift(this.active);
+    this.active = null;
+    this.worker.onmessage = this.worker.onerror = this.worker.onmessageerror = null;
+    this.worker.terminate();
+    for (const task of tasks) { clearTimeout(task.timer); task.reject(error); }
+  }
+}
+
 var IndigoService = function () {
-  function IndigoService(defaultOptions) {
+  function IndigoService(defaultOptions, transport) {
     var _this = this;
     _classCallCheck(this, IndigoService);
@@ -723,18 +796,8 @@
     _defineProperty(this, "ketcherId", null);
     this.defaultOptions = defaultOptions;
-    this.worker = indigoWorker;
-    this.worker.onmessage = function (e) {
-      if (e.data.type === Command.Info) {
-        var callbackMethod = _this.callIndigoLoadedCallback;
-        callbackMethod();
-      }
-      var message = e.data;
-      if (message.type !== undefined) {
-        var event = messageTypeToEventMapping[message.type];
-        _this.EE.emit(event, {
-          data: message
-        });
-      }
-    };
+    this.transport = transport || new NexPolyIndigoTransport(new WorkerFactory());
+    this.worker = this.transport.worker;
+    Object.defineProperty(this, 'disposed', { get: () => this.transport.disposed });
+    Object.defineProperty(this, 'failure', { get: () => this.transport.failure });
   }
   _createClass(IndigoService, [{
@@ -788,7 +851,5 @@
                   }
                 };
-                _this2.EE.removeListener(WorkerEvent.GetInChIKey, action);
-                _this2.EE.addListener(WorkerEvent.GetInChIKey, action);
-                _this2.worker.postMessage(inputMessage);
+                _this2.executeIndigoCallAsync(inputMessage, action, reject);
               }));
             case 1:
@@ -805,28 +866,5 @@
   }, {
     key: "info",
-    value: function info() {
-      var _this3 = this;
-      return new Promise(function (resolve, reject) {
-        var action = function action(_ref2) {
-          var data = _ref2.data;
-          var msg = data;
-          if (!msg.hasError) {
-            var result = {
-              indigoVersion: msg.payload,
-              imagoVersions: [],
-              isAvailable: true
-            };
-            resolve(result);
-          } else {
-            reject(msg.error);
-          }
-        };
-        _this3.EE.removeListener(WorkerEvent.Info, action);
-        _this3.EE.addListener(WorkerEvent.Info, action);
-        _this3.worker.postMessage({
-          type: Command.Info
-        });
-      });
-    }
+    value: function info() { return this.transport.info(); }
   }, {
     key: "convert",
@@ -875,7 +913,5 @@
           data: commandData
         };
-        _this4.EE.removeListener(WorkerEvent.Convert, action);
-        _this4.EE.addListener(WorkerEvent.Convert, action);
-        _this4.worker.postMessage(inputMessage);
+        _this4.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -924,7 +960,5 @@
           data: commandData
         };
-        _this5.EE.removeListener(WorkerEvent.Layout, action);
-        _this5.EE.addListener(WorkerEvent.Layout, action);
-        _this5.worker.postMessage(inputMessage);
+        _this5.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -961,7 +995,5 @@
           data: commandData
         };
-        _this6.EE.removeListener(WorkerEvent.Clean, action);
-        _this6.EE.addListener(WorkerEvent.Clean, action);
-        _this6.worker.postMessage(inputMessage);
+        _this6.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -996,7 +1028,5 @@
           data: commandData
         };
-        _this7.EE.removeListener(WorkerEvent.Aromatize, action);
-        _this7.EE.addListener(WorkerEvent.Aromatize, action);
-        _this7.worker.postMessage(inputMessage);
+        _this7.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1031,7 +1061,5 @@
           data: commandData
         };
-        _this8.EE.removeListener(WorkerEvent.Dearomatize, action);
-        _this8.EE.addListener(WorkerEvent.Dearomatize, action);
-        _this8.worker.postMessage(inputMessage);
+        _this8.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1066,7 +1094,5 @@
           data: commandData
         };
-        _this9.EE.removeListener(WorkerEvent.CalculateCip, action);
-        _this9.EE.addListener(WorkerEvent.CalculateCip, action);
-        _this9.worker.postMessage(inputMessage);
+        _this9.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1103,7 +1129,5 @@
           data: commandData
         };
-        _this10.EE.removeListener(WorkerEvent.Automap, action);
-        _this10.EE.addListener(WorkerEvent.Automap, action);
-        _this10.worker.postMessage(inputMessage);
+        _this10.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1142,7 +1166,5 @@
           data: commandData
         };
-        _this11.EE.removeListener(WorkerEvent.Check, action);
-        _this11.EE.addListener(WorkerEvent.Check, action);
-        _this11.worker.postMessage(inputMessage);
+        _this11.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1185,7 +1207,5 @@
           data: commandData
         };
-        _this12.EE.removeListener(WorkerEvent.Calculate, action);
-        _this12.EE.addListener(WorkerEvent.Calculate, action);
-        _this12.worker.postMessage(inputMessage);
+        _this12.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1248,7 +1268,5 @@
           data: commandData
         };
-        _this13.EE.removeListener(WorkerEvent.GenerateImageAsBase64, action);
-        _this13.EE.addListener(WorkerEvent.GenerateImageAsBase64, action);
-        _this13.worker.postMessage(inputMessage);
+        _this13.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1285,7 +1303,5 @@
           data: commandData
         };
-        _this14.EE.removeListener(WorkerEvent.ExplicitHydrogens, action);
-        _this14.EE.addListener(WorkerEvent.ExplicitHydrogens, action);
-        _this14.worker.postMessage(inputMessage);
+        _this14.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
@@ -1316,14 +1332,22 @@
           data: commandData
         };
-        _this15.EE.removeAllListeners(WorkerEvent.CalculateMacromoleculeProperties);
-        _this15.EE.addListener(WorkerEvent.CalculateMacromoleculeProperties, action);
-        _this15.worker.postMessage(inputMessage);
+        _this15.executeIndigoCallAsync(inputMessage, action, reject);
       });
     }
   }, {
+    key: "executeIndigoCallAsync",
+    value: function executeIndigoCallAsync(message, action, reject) {
+      this.transport.dispatch(message, action, reject);
+    }
+  }, {
     key: "destroy",
-    value: function destroy() {
-      this.worker.terminate();
-      this.worker.onmessage = null;
+    value: function destroy(error) {
+      if (this.disposed) return;
+      this.transport.destroy(error);
+      // Retirement can happen before onInit gives the SDK to the host.
+      this.nexpolyMacroController?.sdk?.retire();
+      this.nexpolyMacroController?.dispose();
+      this.EE.removeAllListeners();
+      this.ketcherId = null;
     }
   }]);
@@ -1346,3 +1370,2 @@
 
 export { STRUCT_SERVICE_INITIALIZED_EVENT, STRUCT_SERVICE_NO_RENDER_INITIALIZED_EVENT, IndigoService as StandaloneStructService, StandaloneStructServiceProvider };
-//# sourceMappingURL=main.js.map
diff --git a/node_modules/ketcher-standalone/package.json b/node_modules/ketcher-standalone/package.json
--- a/node_modules/ketcher-standalone/package.json
+++ b/node_modules/ketcher-standalone/package.json
@@ -81,4 +81,5 @@
   "exports": {
     ".": {
+      "types": "./dist/index.d.ts",
       "import": "./dist/main.js",
       "require": "./dist/cjs/main.js"
