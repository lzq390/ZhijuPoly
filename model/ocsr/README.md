MolScribe checkpoint directory
==============================

Place the MolScribe checkpoint here to enable image-to-structure import.

Expected default file:

    swin_base_char_aux_1m.pth

The backend also accepts `OCSR_MODEL_DIR` as an exact checkpoint file path.
